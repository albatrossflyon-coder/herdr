# BUILDLOG — herdr (this fork)

Fork of `herdrdev/herdr` at `albatrossflyon-coder/herdr`, created 2026-08-21. This file tracks *our* changes/investigation against the upstream project — herdr itself is third-party, not our code, but the fork is ours to develop against before upstreaming.

## What this is
Terminal workspace/multiplexer for coordinating multiple AI coding agents in panes, with a real CLI + socket API for cross-agent orchestration (`agent.prompt`, `agent.wait`, `pane.report_agent`). Local clone: `C:\Repos\herdr` (origin: upstream `ogulcancelik/herdr`, resolves to `herdrdev/herdr`; `fork` remote: `albatrossflyon-coder/herdr`).

## 2026-08-31 (TIC 2) — P0 closed: raw `board move ... Done` / `board done` capability actually removed, not just instruction-forbidden

**Real capability gap confirmed live before fixing it, not assumed.** Started a genuinely fresh Hermes manager pane (`w1:p24`, not the ~19-hour-old `hermes-manager` from earlier) and asked it directly whether it could execute `board move <id> Done` or `board done` via its terminal tool. Its own answer: *"Yes, I have terminal access and could execute those shell commands if I wanted to. The instructions say not to... but technically I'm capable."* Confirms the P0 item flagged in the 3:23 AM entry below was still open — instruction-enforced only, not capability-enforced. Separately re-verified the native-kanban lockdown (`toolsets: []`) from the same entry is still genuinely working — Hermes's own real tool list has zero `kanban_*`/`board_*` entries; the startup banner's "kanban" mention is just a list of known toolset categories, not what's active, and was a false alarm on inspection.

**Fixed the raw board-move-Done capability at the binary level.** Renamed the real `board` CLI (`~/.local/bin/board` → `~/.local/bin/board-real`) and put a wrapper script in its place that refuses, with a clear stderr message pointing to `herdr_controller.py verify`, any of: `board move <id> Done` (by name, case-insensitive, or numeric column id `3`), `board card move <id> Done`, `board done [id] --outcome ok|fail`, `board card run done`, and `board card run confirm` (same completion channel as `run done --outcome ok`). Everything else passes through to the real binary transparently. Patched `herdr_controller.py`'s `board()` helper to call `~/.local/bin/board-real` directly (not the wrapper on PATH) so its own `verify` step remains the one sanctioned caller that can actually reach Done.

**Live-tested both sides, with real throwaway cards, not simulated:**
- All 5 blocked forms refused (exit 1) against a real card (id 23); a non-blocked move (`In Progress`) passed through correctly (exit 0).
- **Accidental real side effect caught and cleaned up:** that `In Progress` test move triggered a real auto-dispatch (the column has `trigger: auto`) — a `pi`-harness run was created against the test card. It failed fast (no live pane spawned, no orphan process), and the card was archived immediately.
- Full sanctioned round trip re-verified end-to-end on a second card (id 24): `submit` → `verify` → real `Done`, confirming `herdr_controller.py` still legitimately closes cards via the direct `board-real` path.

**Known, honest limitation — stated plainly, not overclaimed:** this is a PATH-order wrapper, not an OS-level permission restriction. Hermes has unrestricted shell/terminal access; a deliberately-bypassing agent could still find and execute `board-real` directly by its real path. This closes the *documented, actual* failure mode (an instruction telling Hermes to use the raw command, or a habitual/naive direct call) — it does not cryptographically prevent a determined manual bypass. Chris's explicit call (2026-08-31): ship this now, scope true OS-level restriction (Hermes running under a restricted PATH/user with no execute permission on `board-real`) as a separate follow-up if wanted later.

Commit status: `~/.local/bin/board` (wrapper) and `~/.local/bin/board-real` (renamed real binary) are WSL2 runtime files, not git-tracked. `~/.local/herdr_controller.py`'s `board()` helper patch is the same — WSL2, not in this repo's git tree. Nothing in this git repo changed.

## 2026-08-30 2:28 AM CDT (TIC 1) — P0 control-plane gate built and live-tested: auto-success no longer reaches Done unverified

**Real root cause found and fixed on the live board (board_id 2):** the `In Progress` column's `on_success_column_id` pointed straight at `Done` (id 3) — meaning any auto-dispatched run that reported success landed in Done with zero independent check. This is the exact mechanism behind two of the seven "7 things" items being false completions (Item 1's nonexistent daemon watcher, Item 7's unwritten config edits) — self-report was never checked because there was no state between "succeeded" and "Done" for a check to happen in.

**Fixed:** created three new columns (`Submitted` id 5, `Verifying` id 6, `Rework` id 7) and repointed `In Progress`'s `on_success_column_id` to `Submitted` (was `Done`). A successful run now lands one hop short of Done.

**Built `~/.local/herdr_controller.py`** (WSL2), two commands:
- `submit <card_id> --revision <sha> --file <path> [--file ...] --cmd "<command>::<exit_code>" [--cmd ...]` — requires the card to already be in `Submitted`; records an evidence bundle (revision, changed paths, command+exit-code pairs) to `~/.local/herdr-controller-evidence.json`; moves the card to `Verifying`. Refuses to run against a card in any other column.
- `verify <card_id>` — requires the card to be in `Verifying`; independently re-checks the recorded bundle (every changed path must actually exist on disk, every recorded exit code must be 0, the bundle must be non-empty); only on a full pass does it call `board move <id> Done`. Any failure moves the card to `Rework` with the specific reason posted as a `board comment`, never silently.

**Live-tested against the real board with a throwaway card (id 18, archived after), not simulated:**
1. Submitted fabricated bad evidence (a nonexistent file, a command recorded as exit 1) → `verify` correctly caught both problems and moved the card to `Rework`, comment posted naming the exact failures.
2. Reset the same card to `Submitted`, submitted a real file and a real `exit 0` command → `verify` confirmed the file existed and the exit code was clean, moved the card to `Done`.

**Known, honest limitation — not solved by this change:** the board CLI has no per-caller ACL. Nothing stops Hermes or a worker from calling `board move <id> Done` directly, bypassing `herdr_controller.py` entirely — this closes the *automatic* self-report path, it does not cryptographically prevent a manual bypass. The remaining enforcement is that Hermes's `coding_instructions` and any worker briefing must never grant or mention a raw `board move ... Done` / `board card run done` path, only `herdr_controller.py verify`. That's a policy gap, not a technical one yet; flagged for the P1 pass (giving Hermes/workers a board CLI wrapper with the raw done command actually removed, per the "Herdr Control Plane" artifact's Section 08 P0 item).

**Not yet built:** `dispatch` (health-check + lease + immutable task packet before a card leaves `Todo`), the observer decoupling from Hermes, the heartbeat file for Hermes/Claude Code. This entry covers only the Submitted→Verifying→Done gate.

Commit status: no git-tracked files changed — this is all live herdr-board/SQLite state plus one new script at `~/.local/herdr_controller.py` (WSL2, not in this repo's git tree). Nothing to commit here.

## 2026-08-30 2:43 AM CDT (TIC 1) — Dispatch wrapper built and live-tested; two real pre-existing bugs found and fixed along the way; a third found, documented, not fixed tonight

Continuation of the 2:28 AM entry below. Built the third P0 piece — `dispatch` — and in the process of testing it against the real live roster (not simulated), found real, previously-invisible defects in infrastructure that predates tonight, none of them introduced by this session's work.

**`herdr_controller.py dispatch <card_id> <target> [--ttl-minutes N]` added.** Enforces, in order: (1) refuses a second dispatch onto a card that already has an unexpired lease — reclaims automatically once the lease is expired ("stale-worker-reclaim" from the original 2026-08-24 dispatcher spec); (2) refuses to double-book one worker across two unexpired leases; (3) health-checks the target via `agent.get` before ever touching the board, refusing if herdr can't confirm the agent is alive; (4) records an immutable task-packet hash; (5) moves the card `Todo → In Progress` and prompts the agent via the socket API. Reused the already-tested `HerdrClient` from `~/src/herdr-pilot/herdr_coordinator.py` (Item 6) rather than reimplementing the socket wrapper.

**Live-tested against the real board, three scenarios, all correct:**
1. Fresh dispatch to `copilot-worker` (idle) → lease recorded, card moved to `In Progress`, health check logged `idle` at dispatch time.
2. Re-dispatching the *same* card → correctly refused: `already leased ... refusing a second dispatch`.
3. Dispatching a *different* card to the *same already-leased* worker → correctly refused: `already holds an unexpired lease ... refusing to double-book`.

**Real bug #1 found and fixed: `herdr_coordinator.py`'s `read_agent()` sent `"source": "recent-unwrapped"` (hyphen) but herdr's real API only accepts `recent_unwrapped` (underscore) — confirmed via the live API's own error message.** Every call to `read_agent`, and therefore `AgentRelay`/`TaskPipeline`'s output-reading, has been throwing since the day this file was written. Fixed: `sed -i` the literal string. This means Item 6's "9/9 tests passing" claim never actually exercised the real socket — the test suite mocks at a level that didn't catch this.

**Real bug #2 found and fixed, same file: `read_agent()` returned `result.get("output", "")`, but the real response shape nests it under `result["read"]["text"]`.** Confirmed by printing the raw un-parsed response. Fixed the key path. Between bug #1 and #2, `read_agent` had never once returned real pane content, silently returning empty string in every case regardless of what was actually on screen.

**Real finding #3, documented but NOT fixed tonight (deliberately, per Chris's explicit call not to touch more live panes mid-session): `copilot-worker`'s live process is `node .../copilot --resume=3d075f7e-... ` with *no* `--yolo` or any permission-bypass flag.** Confirmed via `pane.process_info` on its real pane ID (`w1:p1Q`) — first attempt used the agent name as `target` and silently returned a *different* pane's info (hermes's, `w1:p1X`) instead of erroring; a real herdr API quirk (`pane.process_info` wants `pane_id`, not `target`, and doesn't validate). This is the concrete, finally-root-caused version of "copilot always gets stuck needing approval" — a resumed session apparently doesn't inherit whatever flag the original launch used, or none was ever set. herdr's own `agent_status` reports this pane as plain `idle` throughout — `agent.explain` shows `fallback_reason: default_known_agent_idle_fallback`, no rule matched — meaning this exact failure mode (an agent stuck on an unanswerable approval prompt, misreported as idle) is invisible to the health check `dispatch` just built, and was already documented once before for `claw-worker` on 2026-08-25. **Open item: relaunch `copilot-worker` fresh with the correct flag next session, and note that a health check based on `agent_status` alone cannot detect this class of failure — it would need a real round-trip probe (send a trivial prompt, confirm real output within N seconds) to catch it.**

**Re-tested against a genuinely fresh pane, per Chris's explicit correction that testing against `copilot-worker` (already known broken for an unrelated reason) didn't prove anything.** Split a new pane (`w1:p1Z`), started a clean `omp` (Pi) process on it (`dispatch-test-worker`), verified via `pane.process_info` that its actual running command was a plain fresh `bun .../omp` with no stale `--resume` or missing-flag ambiguity. Dispatched card 21 to it: lease recorded, card moved `Todo → In Progress`, and — the piece that couldn't be confirmed against copilot-worker — **read the pane's real screen content and confirmed the exact task text (title, description, instructions) actually landed and the agent visibly entered a `Working…` state.** This is the first real, undeniable confirmation that `dispatch`'s `prompt_agent` call actually delivers into a live pane, not just that the lease bookkeeping is internally consistent.

**Separate, real finding: the fresh agent never completed the trivial task** (reply with the word "ack") after ~48 seconds of waiting, still showing `Working…`. herdr's own `agent_status` reported `idle` the entire time despite the pane visibly showing `Working…` — another real instance of the same status-detection gap already documented for Claude Code/Hermes/copilot. This looks like a model-backend hang (CLIProxyAPI/OmniRoute routing, consistent with prior sessions' documented provider flakiness), not a dispatch-wrapper defect — not chased further tonight, flagged as an open item. Card 21 archived, test pane closed.

**`copilot-worker` fixed for real, per Chris's explicit go-ahead — old broken pane closed, replaced with a fresh, verified-working one.** Split a new pane (`w1:p10`), launched `copilot --yolo` fresh (not resumed), confirmed via `pane.process_info` the real running command included `--yolo`. Smoke-tested with a real shell command (`echo copilot-smoke-test-ok`) — **it ran with zero approval prompt and returned the correct output**, confirmed via the pane's actual screen content, not inferred. This is the real fix for the "copilot always needs approval" pattern, not just a diagnosis. Old stuck `copilot-worker` pane (`w1:p1Q`) closed as superseded.

**New real finding surfaced by the same smoke test: `fff` MCP server fails to start for this copilot instance** — "MCP server process exited before completing the MCP initialize handshake (exit code 1)." Visible directly in the pane's own error message. Not investigated further tonight (separate from the permission-flag fix, and the agent works fine without it for basic tasks) — open item for next session: `/mcp show fff` inside a copilot pane to get the real stderr.

**Test cards 19, 20, and 21 archived, not deleted** (deletion needs `--yes` for non-interactive confirmation, archive is reversible and sufficient for throwaway test artifacts).

Commit status: `herdr_controller.py` (WSL2, `~/.local/`, not git-tracked) gained the `dispatch` function. `herdr_coordinator.py` (WSL2, `~/src/herdr-pilot/`, **no git remote — Item 6's own flagged risk**) got both bug fixes applied directly on disk, uncommitted, because there is nowhere to commit them to yet. This is now a second, real reason to give `herdr-pilot` a git remote soon — losing that directory now loses both the original coordinator work and tonight's two bug fixes with no history of either.

## 2026-08-30 3:23 AM CDT (TIC 1) — P1 pass: requeue command, Hermes config corrected and restarted, one workspace-write limitation confirmed as genuine, one real CLIProxyAPI outage found

**`herdr_controller.py requeue <card_id> [reason]` added.** Clears a card's lease and evidence, comments the reason, moves it back to `Todo`. This is the sanctioned recovery path for a stuck/dead worker — the alternative (a human manually moving columns while a stale lease sits in the file, silently contradicting the board) is exactly the kind of drift this control plane exists to remove. Tested live: refuses on a card already in `Done`, no-ops safely on a card with no existing lease.

**Real, honest finding: "remove Hermes's workspace write access" (P0 item) is not achievable via config.** Checked `~/.hermes/config.yaml` in full — `command_allowlist` is a single freeform note ("script execution via -e/-c flag"), not a real ACL; there is no `tools:`/`permissions:` section anywhere. Hermes has no native mechanism to restrict its own file-write capability short of not being told to use it. This is a genuine platform limitation, not a task left undone — recorded here so it isn't silently re-attempted as if it were solvable by a config tweak.

**Fixed a live, active landmine in Hermes's own standing instructions.** `~/.hermes/config.yaml`'s `coding_instructions` still told Hermes, verbatim, to close cards via `board card run done` / `board move <id> <column>` directly — the exact bypass the new Done-gate exists to prevent. Added two `CORRECTION 2026-08-30` entries (via `yaml.safe_load`/`safe_dump`, not raw text insertion, per this repo's own prior lesson about raw YAML edits corrupting neighboring blocks) pointing Hermes at `herdr_controller.py dispatch/submit/verify` instead, plus a third instruction: write a heartbeat timestamp to `~/.local/hermes-heartbeat.txt` on every poll cycle, since herdr's own `agent_status` for Hermes itself is confirmed screen-manifest-only, not a real lifecycle signal.

**Killed and restarted `hermes-manager`** (old pane `w1:p1X` closed, fresh one on `w1:p21`) so the corrected config actually loads — a live process does not pick up a config edit on disk, already a documented gotcha. Config file verified valid YAML with all 13 instructions present before the restart.

**Live behavioral verification of the new instructions is blocked by a real, narrow, WSL2-specific network-path problem — CLIProxyAPI itself was never down.** Prompting the fresh Hermes hung indefinitely on `omniroute-auto` (also reproduced independently on a fresh Pi pane earlier this session — two unrelated agents, same hang, same model route). Direct `curl` from **inside WSL2** to `http://192.168.144.1:8317/v1/models` and `/v1/chat/completions` both returned connection failures (`HTTP_STATUS:000`, curl exit 28). **Important correction — the service itself was confirmed running, listening on `0.0.0.0:8317`, and connectable the entire time** (verified via the EasyCLIProxyAPI desktop console showing Running/Connectable, and via Windows-side `netstat` showing the port actively LISTENING) — an earlier verbal report calling it "down" was imprecise and prompted an unnecessary manual restart (PID 22952 → 10832) that changed nothing about the underlying WSL2-reachability problem, confirmed by re-testing after the restart with the identical `HTTP_STATUS:000` result. Ruled out: automatic/periodic PID-cycling (no scheduled task matches CLIProxyAPI/EasyCLIProxyAPI; the wrapper app `EasyCLIProxyAPI.exe` has been running continuously since the previous evening with no restart-related config found in its files) — the one PID change observed tonight was a one-time manual restart, not recurring background behavior. **The actual scope: something on the WSL2 vEthernet network path (not the service, not the process, not a crash-loop) is silently dropping WSL2-originated connections to Windows port 8317.** Not root-caused tonight — needs a fresh pass checking the firewall rule's exact profile/interface scope and the WSL vEthernet adapter's current network category, not more curl retries from the WSL2 side. **This is the same `omniroute-auto` timeout flagged as open in last night's (2026-08-30 TIC 2) session — now confirmed as a WSL2-side network-path issue specifically, not a provider or service outage, narrowing where the next session should actually look.**

**UPDATE, 3:39 AM CDT — root cause found and fixed** (see the dedicated entry below with the full diagnosis). **UPDATE, 3:45 AM CDT — the blocked verification above is now complete.** Switched `hermes-manager` to the confirmed-working `omniroute-auto` and re-asked the same verification question: it correctly answered with the real `submit`/`verify` path (explicitly naming that `board done`/`board card run done`/`board move <id> Done` bypass the evidence check) and the heartbeat-file requirement, verbatim-accurate to what was written into its config. The heartbeat file (`~/.local/hermes-heartbeat.txt`) doesn't exist yet, which is expected — Hermes only writes it while actively polling as manager, and this was a knowledge-check question, not a real dispatch cycle; will confirm it gets written once Hermes is actually managing real work.

Commit status: `herdr_controller.py` (WSL2, not git-tracked) gained `requeue`. `~/.hermes/config.yaml` (WSL2, not git-tracked, runtime config) gained the three new instructions. Nothing in this git repo changed.

## 2026-08-30 3:39 AM CDT (TIC 1) — ROOT CAUSE FOUND AND FIXED: WSL2 could not reach CLIProxyAPI at all, blocking every model call for every herdr agent

**This is the actual root cause of tonight's (and last night's, per 2026-08-30 TIC 2's open item) `omniroute-auto` timeouts.** Not a provider issue, not a stale process, not a version bug — a firewall rule scoped to the wrong network profile.

**Diagnosis, via `start-to-finish` (invoked properly this time, after being asked directly whether it had been used):**
- Ruled out: service down (confirmed running/listening/connectable throughout, on both the old and a freshly-updated Proxy Core Kernel — v7.2.143 and v7.2.145 both hit the identical failure, so the update was not the fix).
- Ruled out: this session's own unrelated port-8842 dashboard networking work (checked the full `netsh interface portproxy show all` table — one entry, scoped to 8842 only, no overlap).
- Ruled out: third-party antivirus/firewall (`Get-CimInstance -Namespace root/SecurityCenter2` confirmed only Windows Defender registered, no separate firewall product).
- Confirmed via web research: WSL2's vEthernet adapter is classified by Windows as an "Unidentified Network," which defaults to the **Public** firewall profile — a well-documented WSL2 networking gotcha (microsoft/WSL#4139 and related issues).
- **Confirmed directly:** `Get-NetFirewallRule -DisplayName "CLIProxyAPI (WSL2 access)"` showed `Profile: Private` — the rule that was supposed to let WSL2 through had never actually applied to WSL2's own traffic, despite being Enabled and looking correct at a glance.

**Fix:** `Set-NetFirewallRule -DisplayName "CLIProxyAPI (WSL2 access)" -Profile Any` (run elevated, by Chris — this tool cannot self-elevate). Confirmed why the same class of bug didn't hit this session's own dashboard rule: `New-NetFirewallRule` with no `-Profile` argument defaults to Any; the CLIProxyAPI rule had been created at some point scoped explicitly to Private only, likely via the Windows Firewall GUI's default "Private networks" checkbox.

**Verified fixed with the real thing, not a proxy for it:** a full authenticated `curl` to `/v1/chat/completions` with `model: omniroute-auto` — the exact route that hung all night — returned a real streaming completion (`"ok"`, routed to `big-pickle` via provider `oc`, 15ms latency, HTTP 200). Before the fix: `HTTP_STATUS:000` (connection never completed). Immediately after the profile fix, before adding auth: `HTTP_STATUS:401 Missing API key` — proving the network path itself was already fixed, auth was the only remaining gate.

**Scan/test steps (per start-to-finish):** scan doesn't apply — this is a live Windows Firewall config change, not code. The real completion above is the test; no synthetic check would have proven this.

**Commit/ship status:** no git commit or push applies — Windows Firewall rule state isn't a tracked file. This BUILDLOG entry is the standing record.

**Real, concrete unblock:** every herdr agent (Hermes, workers, anything routing through CLIProxyAPI's `omniroute-auto` alias) can now actually reach a model. The Hermes config corrections from earlier tonight (3:23 AM entry — the Done-gate correction and heartbeat instruction) can now actually be behaviorally verified, since the thing blocking that verification is fixed.

## 2026-08-27 — Real incident: this repo was moved into `running-repos\`, broke 4 live worktrees, moved back

Discovered mid-session while investigating unrelated `herdr-board` work: this repo had been moved from `C:\Repos\herdr` to `C:\Repos\running-repos\herdr` during the 2026-08-25/26 (TIC 1, part 3) C-drive reorg session. That move broke all four live linked worktrees — `herdr-aura`, `herdr-claw`, `herdr-cline`, `herdr-crush` — because a git worktree's `.git` file hardcodes the *absolute path* back to the main repo's `.git/worktrees/<name>` (here, a WSL2-style path: `/mnt/c/Repos/herdr/.git/worktrees/<name>`). Once the main repo moved, that path stopped existing and every worktree failed with `fatal: not a git repository`. This went unnoticed for roughly a day — same failure category as the job-hunter incident the same reorg night (a moved path breaking a hardcoded reference elsewhere), just not caught until now.

**Fixed by moving the repo back to `C:\Repos\herdr`** — verified all four worktrees resolve correctly again (`git status` succeeds on all four, real branch/file state intact, nothing lost).

**PINNED — do not move this repo again without first checking `git worktree list` for absolute-path dependents.** If it ever needs to move, the safe order is: move first, then run `git worktree repair <path-to-each-worktree>` from inside the main repo to update the links — do not just move and assume worktrees will still resolve.

**Explicit flag for next herdr restart:** pay close attention when herdr/its agents next start up — confirm the four worktree-based agent panes (aura, claw, cline, crush) actually reconnect cleanly to their worktrees now that the path is restored, since they may have been in a broken state for about a day.

## 2026-08-26 (TIC 2) — CLIProxyAPI/EasyCLIProxyAPI cross-reference

herdr's roster (all agents — Claude Code, Codex, cline, kilo, claw, aura-code, etc.) routes model calls through **CLIProxyAPI**, a Windows-hosted proxy (`C:\Repos\CLIProxyAPI`, pinned — a live process runs directly from that folder, never move it). WSL2 reaches it over the network via a firewall rule, not a local WSL2 install — this is *not* an `installed-in-wsl2\` candidate despite being roster-critical.

Now manageable via **EasyCLIProxyAPI** (`C:\Repos\EasyCLIProxyAPI.md`), the official desktop GUI — installed 2026-08-26 (portable Windows release, no installer). Its **Agent client configuration** page auto-detects and one-click-wires installed clients, explicitly including Hermes Agent and Pi from the actual roster, alongside Claude Code/Codex/OpenCode/OpenClaw/etc. Chosen over `CPA-Manager-Plus` (deeper monitoring/automation, but no client-side wiring at all).

## 2026-08-26 (TIC 2) — Scrapling identified as agy's real scraping gap-filler, not Apify — open item

Came up while deciding whether Apify's MCP server would help agy with scraping (e.g. YouTube research for content). Conclusion: agy already has native `/agy:scrape` + `/agy:video`, plus the `watch` and `gemini-video` skills cover video research — Apify (`apify-mcp-server`, kept as a reference clone in `running-repos\`) would be redundant and costs money per Actor run. **Scrapling** (`https://github.com/D4Vinci/Scrapling`, free/open-source, stealth web-fetching with anti-bot handling) closes the real remaining gap — hard/anti-bot-protected targets — for $0, and it's already proven useful enough to be installed on the Windows Claude Code side today (`uv tool install "scrapling[all]"` + `scrapling install`, MCP server registered).

**Not yet installed in WSL2 for agy/the roster.** Install command: same `uv tool install "scrapling[all]"` + `scrapling install`, run inside WSL2, then wire the resulting `scrapling mcp` command into agy's MCP config (`~/.gemini/antigravity/mcp_config.json`, the same file `antigravity-project-starter` configures) or Hermes's own MCP config. Full detail: `C:\Repos\installed-in-wsl2\Scrapling.md`.

## 2026-08-26 (TIC 2) — antigravity-project-starter found, needs install on the agy agent — open item

Found `antigravity-project-starter` (`C:\Repos\installed-in-wsl2\antigravity-project-starter\`) during the repo-cleanup pass — a starter kit that bundles `jcodemunch` (AST code retrieval) and NotebookLM MCP access into a 5-gate spec-driven workflow, built for Google's Antigravity CLI (`agy`).

**Confirmed this is the right agent to install it on:** herdr's own `src/detect/manifests/antigravity.toml` detects the `agy` agent (`id = "agy"`, aliases `antigravity`/`antigravity-cli`) via terminal-text pattern matching, and its installed lifecycle hook (`src/integration/assets/antigravity_cli/herdr-agent-state.sh`) is a POSIX `#!/bin/sh` script — the real `agy` process herdr manages runs inside WSL2, same as claw/codex/cline.

**Install:** `npx antigravity-awesome-skills` **run from inside WSL2** (not Windows) — it injects MCP server definitions into `~/.gemini/antigravity/mcp_config.json`, which only resolves correctly from WSL2's own home directory. This is global (affects every `agy` session), confirmed fine with Chris.

**Not yet installed.** Full detail: `C:\Repos\installed-in-wsl2\antigravity-project-starter.md`.

## 2026-08-26 (TIC 2) — flock-check skill found, not installed anywhere — open item

Found `flock-check` (`C:\Repos\_scan-flock-check\SKILL.md`, v1.0.0, authored by "Hermes Agent") during a repo-cleanup pass — a read-only skill that checks a live herdr server/workspaces/agent states and returns an evidence-grounded status summary (who's working/idle/blocked/needs input) without touching any agent. `platforms: [linux, macos]` in its own frontmatter, so it only runs where herdr itself runs (WSL2), not from native Windows.

**Not currently installed or wired up anywhere in the roster.** Recommendation: install on whichever agent plays the HQ/coordinator role (Hermes) — the skill's own scope explicitly excludes "the current Hermes/HQ agent" from what it checks, meaning it's designed to run *from* the coordinator *on* everyone else, not something a worker agent (Pi, Codex, cline, claw, kilo, aura-code) would use. Not yet actioned — full detail also tracked in `C:\Repos\installed-in-wsl2.md`.

## 2026-08-25 (TIC 1) — Manager-architecture corrections, "7 things" progress (3 done, 1 partial, 2 real bugs caught), real git-push hang open

**Manager architecture, corrected in real time.** Wrote a new living operating-procedure doc, `~/.claude/skills/herdr-orchestrate/herdr.md`, wired into that skill's SKILL.md so it loads automatically. Real process failures caught and fixed this session, each one now codified as a rule in that file: (1) told hermes-manager to implement work itself instead of delegating — corrected; (2) bypassed hermes-manager entirely and drove workers directly myself — corrected, handed dispatch fully back; (3) was reading live herdr state through raw CLI via Bash/wsl.exe, which has no timeout and can hang the session — switched to `mcp__herdr__*` tools only; (4) never checked for a prior-owner session before spawning fresh workers — real gap, only partially fixed (found hermes-bunfix's actual resumable session, `20260821_182729_251ee1`, never actually resumed it).

**herdr server crashed once mid-session** (real, unexplained — not the known nohup/setsid bug, RAM was fine) during a rapid batch of pane close/recreate calls. Recovered via `setsid nohup herdr server ...`. `hermes-manager` and `copilot-worker` survived via herdr's own live-handoff (same session, no data lost); `codex`, `agy`, `cline`, `kilo`, `claw` all needed relaunching. Board state (SQLite-backed) survived fully intact regardless.

**7-things status, real verification not just self-report, per item:**
- **Item 3 (session-restore) — fully done.** Claw+cline resume support: commit `93ff903` verified pushed to `fork/feature/four-agent-integration` (`git branch -r --contains`, not just local log). Found and fixed a second real piece left over in the same worktree (`Agent::ALL`/`SCREEN_MANIFEST_AGENTS` array-size bumps + claw/cline session-reporting hook scripts) — isolated from ~1200 files of line-ending noise via `git diff -b`, committed `e1d95e7`, vuln-hunter scanned clean. **Push to `fork` is currently hanging/timing out (exit 124, zero output) — root cause not found, not yet verified on GitHub. Open item.**
- **Item 6 (agent coordination) — done.** agy built a real `herdr_coordinator.py` (HerdrClient/EventListener/AgentRelay/TaskPipeline), 9/9 tests passing, verified live against the real socket. vuln-hunter scanned clean, committed locally in the `herdr-pilot` scratch workspace (no remote there, so local-only).
- **Item 1 (event-driven automation) — done, after two failed handoffs.** cline hit a genuine recurring API-protocol bug (`"text part ... not found"`) across two different models — real bug, not provider flakiness, flagged for a later look, not retried further. agy picked it up next but hit full account quota exhaustion (all three model tiers — Flash, Claude Sonnet, GPT-OSS — exhausted same night). Landed with kilo: found what event types were already wired vs. unused, wired `pane.output_matched`, real tests passing (15/15 board-herdr, 28/28 daemon watcher).
- **Item 7 (MCP profile optimization) — partially real.** kilo's own summary claimed real config changes; direct verification found **zero** of them actually written to any config file — pure self-report, not a real edit. Applied the concrete, low-risk part directly (`fff --no-update-check`, `obscura --stealth` in `~/.hermes/config.yaml`, confirmed via live MCP reconnect log after a model switch). The larger piece — disabling 7 redundant handle-list tools on jdatamunch/jdocmunch — needs exact tool-name strings kilo has and I don't; still open.
- **Item 4 (worktree isolation) — real commit exists, full completion unclear.** Copilot committed `spin-agent.sh` for real, but kept hitting recurring permission-gate loops needing active clearing — status at session end uncertain, needs a fresh check.
- **Item 2 (plugin system) — genuinely in progress, not stuck.** claw real-investigating actual herdr source (`schema.rs`, `plugin_registry.rs`, `server/mod.rs`). Real launch-flag gap found: `--dangerously-skip-permissions` does not prevent claw's workspace-write→danger-full-access escalation gate on bash commands — needs `--permission-mode danger-full-access` explicitly next time.
- **Item 5 (remote/phone attachment) — real bug caught before it shipped.** Pi built `herdr_remote.py`, board marked it Done — direct code read found `cmd_attach()` uses `subprocess.INHERIT`, which doesn't exist in Python's stdlib and would crash on every real use. Sent back to Pi with the exact fix (`None` instead) plus a flag on an unverified install URL (`sapiens-ai/herdr`, doesn't match any known repo referenced tonight). Not yet re-verified as fixed.

**Line-ending corruption, real root cause found, one repo fixed.** No `.gitattributes` normalization rule exists in this fork; something round-tripped `~/build/herdr-four-agent-src`, `~/build/herdr-crush-src`, `~/src/herdr-build`, and the Windows-side `C:\Repos\herdr` clone all through a line-ending flip, making `git status` show ~1200 files as modified in each. Confirmed via `git diff -b` (ignores line endings) that it's 100% noise in `herdr-four-agent-src` except one real 2-line change. Fixed there via `git stash` (not `git checkout --`, which this machine's safety net blocks) + reapply of the isolated real patch. **The other three affected trees still need the same treatment — open item.**

**CLIProxyAPI: 5 new keys added tonight** (Mistral #5, BazaarLink #3, Z.AI #2 "Claude Code Army", Z.AI #3 "albatrossaionline", NVIDIA #3 "Claude Code Army") after hitting real, sequential provider failures — Cerebras (out of credit), Mistral (503 auth), Grok/aihubmix (403 balance), Z.AI x2 (429 balance, then a paid-tier mistake on `glm-4.6`), BazaarLink (free daily limit). Settled on `nararouter-agnes` (slow but reliable) for most of the session, later `glm-4.7` (genuinely free tier, confirmed via config comments — `glm-4.6`/`glm-5.3` are marked paid despite being on a "free-tier" account). A real xAI/Grok key was also provided but the account has zero credits/licenses purchased — not usable yet, not added to config.

**Real open items for next session:** fix the `git push` hang on `fork/feature/four-agent-integration` (commit `e1d95e7` sitting local-only); verify Pi's `herdr_remote.py` fix; get kilo to finish item 7's tool-disabling with real tool names; check item 4/Copilot's actual final state; finish item 2/plugin-system; clean up the other 3 line-ending-corrupted trees; wire CLIProxyAPI→OmniRoute once a consumer key exists; fund or drop the xAI key.

## 2026-08-25 (TIC 1) — TODO: hook CLIProxyAPI up to OmniRoute for automatic model/key fallback

**RESOLVED 2026-08-25 (TIC 1, later same session) — done, verified live end-to-end.** See the dated entry below for the real key-minting path used and verification evidence.

Real, scoped next step, not yet done. OmniRoute (Fly.io, `https://albatross-omniroute.fly.dev`) is already live with 8 real provider connections (openrouter, openai, mistral, groq, agnes, glm, plus 2 audio) and a native 4-tier auto-fallback cascade (Subscription → API Key → Cheap → Free) with circuit breakers and key cooldown built in — see vault `Projects/omniroute.md` for the full deployment history.

**How to wire it in:** CLIProxyAPI already supports adding any OpenAI-compatible service as an upstream provider (same pattern as its existing `openrouter`/`mistral`/`groq` blocks in `CLIProxyAPI/config.yaml`). Add a new provider block:
```yaml
- name: "omniroute"
  base-url: "https://albatross-omniroute.fly.dev/v1"
  api-key-entries:
    - api-key: "<consumer key>"
  models:
    - name: "auto"
      alias: "omniroute-auto"
```
Once wired, every agent already routing through CLIProxyAPI gets one more hop that internally auto-falls-back across all 8 of OmniRoute's connections before ever failing outright — real relevance given tonight's session hit provider failures on Cerebras, Mistral, Grok, Z.AI (x2), Gemini, and BazaarLink, one after another.

**What's blocking it:** need a *consumer* API key (different from the admin bearer token used to manage providers) — generated from OmniRoute's own web dashboard (`Dashboard → Endpoints`), or mintable via the admin token (`oma_live_M0Hk-...`, minted 2026-08-19/20, only the truncated form is in vault memory — full value lives in a raw session transcript backup not yet located).

**Also unresolved from 2026-08-21:** whether to fully consolidate CLIProxyAPI's key pool onto this Fly.io instance, or keep them split across agents for double the effective quota headroom — real tradeoff, not decided.

## 2026-08-25 (TIC 1, later same session) — Fixed 2 real stuck herdr agents; root-caused hermes-manager's zai failure; wired CLIProxyAPI→OmniRoute

**Roster survey via `mcp__herdr__*` tools (per herdr.md, never raw CLI):** found 8 live agents in workspace `w1`/`herdr-pilot`. Two were genuinely stuck, not just idle:

- **hermes-manager**: hit 3x `HTTP 429 insufficient balance` on model `glm-4.7` via provider `zai`, then a context-compaction timeout (595 msgs, ~171k tokens). Root cause was **not** the keys — Chris's freshly-added Z.AI keys (#2, #3) were genuine and unused. Real cause, found in two layers: (1) `cli-proxy-api.exe` (PID 15096, started 8/24 4:01 PM) had been running since *before* `config.yaml` was edited at 8/24 11:52 PM to add those keys — a live process never picks up a config edit, confirmed via `Get-CimInstance`/file mtime comparison, same class of bug as the jMunch family's known non-hot-reload behavior. (2) Even after restarting the process, hermes kept requesting model `glm-4.7` (no `-flash`), which isn't in `config.yaml`'s alias list at all (only `glm-4.7-flash`/`glm-free` is marked genuinely free) — CLIProxyAPI passes unaliased names straight through to Z.AI's real API, hitting the paid tier with a free-tier key regardless of which key rotates in. Confirmed via a direct `curl` to `glm-free`, which worked cleanly post-restart, while hermes' own retry on `glm-4.7` failed identically. Fix: restarted `cli-proxy-api.exe` (new PID, correct working dir `C:\Repos\CLIProxyAPI`) to pick up the real key pool, then switched hermes off zai entirely via `/model nemotron-ultra --provider openai-api` (hermes' own zai-scoped model catalog turned out to be stale too — offered only raw upstream names like `glm-4.7`/`glm-5.3`, never CLIProxyAPI's aliases, matching the known gotcha in herdr.md §9a). Verified recovered: hermes accepted a real prompt and began working correctly (ran flock-check first, per procedure) with no further errors.
- **claw-worker**: genuinely blocked on an unanswered `Approve this tool call? [y/N]:` prompt for a read-only `grep` (checking `temp_env` usage in `herdr-build/src/api/schema/tests.rs`) — herdr's own detection heuristic mislabeled this pane "idle" (a fallback rule, not a real match on the blocked-prompt evidence). Root cause: launched with `--dangerously-skip-permissions` instead of `--permission-mode danger-full-access` (already-known gotcha, herdr.md §9a). Approved the pending grep directly to unstick it; flagging the detection-heuristic miss as a possible manifest gap worth a closer look, not fixed here.
- **cline-worker**: closed — wedged on the same unrecoverable "not found" error from last session's known cline API-protocol bug, and its card (Item 1, event-driven automation) was already completed via kilo. Dead pane, no unique work lost.

**Board reality-check (direct `board card list --json`, not routed through hermes since it was still recovering):** of the 7 "Item N" cards, board `status` disagreed with what was actually verified last session on 4 of them (matches herdr.md §9b's warning that card status can lie) — Items 1/3/6/7 show `failed` on the board but were independently verified done via a different path (kilo, direct commit, live test) last session. Items 2 (plugin system) and 4 (worktree isolation) are genuinely still open, matching prior notes. **Item 5 (remote/phone attachment) card says `done` but is not** — last session caught a real bug in Pi's `herdr_remote.py` (`subprocess.INHERIT`, doesn't exist in Python) and sent it back unfixed; the board was never corrected. Real open item: reconcile these 4 stale cards once hermes is fully stable.

**CLIProxyAPI→OmniRoute wiring, completed** (was the open TODO directly above this entry). Read OmniRoute's own source (`src/app/api/keys/route.ts`, `src/app/api/auth/login/route.ts`) rather than guessing the dashboard flow: logged into the Fly.io instance via `POST /api/auth/login` using the known `INITIAL_PASSWORD` (from `omniroute-source/.env`), then minted a real consumer key via `POST /api/keys` (defaults to `self:usage` scope, no admin/manage rights) — no need for the never-fully-recovered admin token. Verified the key directly against OmniRoute (`POST /v1/chat/completions`, model `auto` → routed to `oc/hy3-free`, real streaming response, $0.00 cost) before wiring it into `CLIProxyAPI/config.yaml` as a new `omniroute` provider block (model `auto`, alias `omniroute-auto`). Restarted CLIProxyAPI once more to load it, then verified the full chain through CLIProxyAPI itself (`omniroute-auto` → real completion, `oc/big-pickle`, $0.00). Every agent already routing through CLIProxyAPI now has one more real fallback hop before ever failing outright.

**Status:** hermes-manager recovered and working; claw-worker unstuck; cline-worker closed (dead); CLIProxyAPI→OmniRoute live and verified. Open: reconcile the 4 stale board cards; re-verify Pi's `herdr_remote.py` fix for Item 5; claw's detection-heuristic miss (idle vs. blocked) not investigated further. No app code changed this entry — infra/config only (process restart, YAML provider block, board read), so the usual scan/test steps don't apply; verification was direct `curl` calls at each layer instead, all shown above. `config.yaml` is gitignored in `CLIProxyAPI` (per existing repo convention) so no commit there; this `BUILDLOG.md` entry is the record.

## 2026-08-25 (TIC 1, session wrap-up) — Board reconciled, 4 remaining bugs dispatched, disk-space forced an early stop

**Board reconciliation, done via hermes then finished directly:** Items 1/3/6/7 corrected to their real Done placement with evidence comments. Item 5 (remote/phone attachment) independently re-verified myself — `grep INHERIT` on the real `herdr_remote.py` (found at `~/src/herdr-pilot/herdr_remote.py`, not the path the card description pointed at) returns zero matches, confirmed fixed, moved to Done with the verification comment attached (card #56).

**Real dispatch of the 3 remaining broken-agent items, all genuinely in progress, none confirmed complete:**
- **OpenCode's Bun crash** — copilot-worker got stuck twice on repeated dialog loops; per a new standing rule (swap out a repeatedly-stuck agent instead of babysitting it), hermes substituted **aura**, which reports `done` on the fix. Not independently re-verified this session — next session should confirm by actually relaunching a fresh OpenCode instance, not just trust aura's self-report (per herdr.md §9b).
- **cline's API-protocol bug** — a genuinely fresh `cline-test` instance was launched specifically to reproduce it live (not just investigated secondhand). It worked: reproduced `Error: text part msg_chatcmpl-..._0 not found` twice, live, confirming the bug is real and still present. Root cause not yet found. codex-worker was separately investigating the same bug in parallel; both were told to checkpoint at session end.
- **Pi/OpenCode board-auto-dispatch bug** — pi-worker was dispatched to investigate herdr's own board-integration code. No result reported before wrap-up.
- **Item 4, worktree isolation (card 11)** — claw-worker never made real progress on this. Confirmed live: claw has needed a manual `y`/`N` approval for *every single tool call* all session, the same root cause flagged earlier (wrong launch flag, `--dangerously-skip-permissions` instead of `--permission-mode danger-full-access`) — its `state_change_seq` never moved from the value it had before this session's work even started. This is a real, unfixed operational gap: claw is effectively unusable for unattended work until relaunched with the correct flag. Do that first, next session, before assigning it anything else.

**A brand-new Claude-kind agent got started for card 11 without authorization** — Hermes picked "claude" from two options it was given, without first confirming which account it would authenticate against. Caught before any real use: the pane showed `Login expired · Please run /login` and never got past that, so zero usage was drawn. Closed. **Real process gap, not yet fixed:** dispatch instructions that offer an agent-kind choice should also state the account/cost implication up front, not leave it for whoever's picking to discover after the fact.

**hermes-manager hit a hard, unrecoverable failure near the end:** `nemotron-ultra` (NVIDIA's free model) started failing with `503 auth_unavailable` intermittently, then confirmed **not** transient — tested directly against NVIDIA's own API (bypassing CLIProxyAPI entirely), same 503, same key: a real outage on NVIDIA's free-tier NIM service, external and unfixable from here. Switched hermes to `groq-oss` (verified working), which required a context re-compaction — that compaction then hit `413 Request payload too large. Cannot compress further` after ~7.75 hours of accumulated session history. Unrecoverable without a full session reset (which would lose hermes' own memory of the night). The remaining checkpoint instructions were sent directly to each worker instead, bypassing hermes for this one wrap-up step out of necessity, not preference.

**Session-end handoff, explicit, per herdr.md §7:**
- **kilo-worker** (card 17, Open Interpreter/Kimi K3 install) — mid-install when checkpoint instruction was sent (was hunting `openssl`/`libssl` build deps, no conda available). Resume: check `board card show 17` for its comment, or re-attach pane `w1:p1N`.
- **codex-worker** (cline's bug, parallel investigation) — working ~2m24s in on the bug when checkpointed. Resume: `board card show 14` or pane `wC:p1`.
- **cline-test** (cline's bug, direct reproduction) — confirmed the bug reproduces live; was mid-checkpoint-commit when session ended. Resume: worktree `~/.herdr/worktrees/herdr-pilot/test-isolation-1787538627`, pane `w9:p2`.
- **claw-worker** (card 11, worktree isolation) — no real progress; blocked on approval gates the entire session. Resume only after relaunching with `--permission-mode danger-full-access`.
- **pi-worker** (Pi/OpenCode board-auto-dispatch bug) — just dispatched, no progress reported before wrap-up.
- **aura** — reports done on the OpenCode fix, unverified.
- **hermes-manager** — session hit a hard 413 wall, needs a fresh session next time (`hermes --resume` will likely re-hit the same payload-size ceiling immediately; consider starting clean instead).

**Why this stopped here:** C: drive dropped from 100+GB free to ~41-42GB free over two days (Docker's 53GB vhdx + WSL2's 61GB vhdx, neither auto-shrinks even after `docker system prune` reclaimed 15.19GB + ~4GB of named volumes internally — the vhdx files themselves stay their old size until an explicit `wsl --shutdown` + compact). Chris chose to have active workers checkpoint before shutting down rather than losing in-progress reasoning state outright. WSL2 shutdown + VHDX compact is the immediate next action after this entry lands, separate from herdr work itself.

## 2026-08-24 8:09 PM CDT (TIC 2) — TODO for Hermes: check whether `google-antigravity/antigravity-cli` is already installed/working

Real, concrete lead, not speculative: herdr's own bundled agent manifest for Antigravity (`src/detect/manifests/antigravity.toml`) declares `id = "agy"` with `aliases = ["antigravity", "antigravity-cli"]` — meaning herdr's detection logic was **already built assuming** a CLI binary named `antigravity-cli` (or `antigravity`/`agy`) exists on PATH. This isn't a new capability to evaluate; `github.com/google-antigravity/antigravity-cli` (found 2026-08-24, not yet cloned/scanned) is very likely the real, official implementation of the exact tool this manifest already expects.

**Open question for next session, to check directly rather than guess:** is `antigravity-cli`/`agy` already installed and working in the WSL2 environment (the `antigravity:agy-rescue` Claude Code plugin already references an `agy` CLI via `--print` mode, so *something* by that name may already be reachable from Claude Code's side, but that doesn't confirm it's on PATH for herdr's own pane-based detection to work against). If it's missing or non-functional, this GitHub repo is the concrete fix — clone, scan (standing rule), and install.

Side note, not yet verified: Chris flagged that Google's Antigravity product may now be built on/branded around Gemini rather than being model-agnostic — worth confirming what model(s) actually back it before assuming anything about its output quality relative to other agents in the roster.

## 2026-08-24 7:20 PM CDT (TIC 2) — OpenClaw: candidate roster addition, not evaluated yet, real install guidance from a real source

Already cloned at `C:\Repos\openclaw` (shows up in `repo-update-automation.ps1`'s pull list) but **not installed, not run, not scanned, not wired into herdr** — just sitting on disk. Not yet decided whether it belongs in the roster; this entry only captures real install guidance so the eventual attempt doesn't repeat known mistakes.

**Real install guidance from a walkthrough video (not guessed):**
- OpenClaw has two install paths: native (`npm install -g openclaw@latest` or an install script) and Docker. The video's explicit recommendation is native-first: "for your daily driver, install native is the smoother path... reach for Docker when you actually need reproducibility, a VPS, a Kubernetes deployment, a CI test rig." Docker is not the default or required path.
- On Windows specifically: "you should be running OpenClaw inside WSL2, period. Do not run it natively on Windows." So the right combination for this machine is **native npm install, run inside WSL2** — not Dockerized, not native-Windows. Same shape as every other agent already in the roster (Codex, Hermes, etc. are all native WSL2 builds, not containers).
- **Critical, already-learned-the-hard-way overlap**: the video separately warns that OpenClaw's state must live on WSL2's native filesystem (`~/.openclaw`, i.e. `/home/<user>/.openclaw`), never under `/mnt/c/Users/<user>/.openclaw` — "the `/mnt/c` path is a shim over NTFS with terrible permission semantics... you will lose half a day debugging it." This is the exact same bug class already hit and fixed with herdr's own cargo/Zig builds (`/mnt/c` broke the Zig cache; fix was moving worktrees to `~/build/<name>-src`). Independent confirmation this is a general WSL2 gotcha, not something specific to Rust builds — treat "never touch `/mnt/c` for anything stateful" as a blanket rule for every future WSL2 tool, not just cargo.
- If Docker ever does get used for this or anything else in WSL2: known bind-mount gotcha is a UID/GID mismatch between the host directory owner and the container's user, causing a silent permission-denied on first write. Fixes, in order of preference: `chown -R 1000:1000` on the host dir, or `--user $(id -u):$(id -g)` on `docker run`, or use a named Docker volume instead of a bind mount.
- OpenClaw ships its own `openclaw doctor` diagnostic command — "way faster than poking around by hand" per the same source. Worth running first if an eventual install attempt hits trouble, before manual debugging.

## 2026-08-24 6:41 PM CDT (TIC 2) — TODO for Hermes: install Open Interpreter (Kimi K3 harness) and CodeGraph across the roster

Not done this session — deliberately left for Hermes to do directly inside herdr's WSL2 environment next session, since both tools are meant to run there (agent binaries/MCP servers), not on the Windows Claude Code side. Both evaluated, scanned, and fp-checked clean on the Windows side first; only the actual install/wiring step remains.

**1. Open Interpreter (`github.com/openinterpreter/open-interpreter`) — install and wire in, with a focus on the Kimi K3 harness.**
- What it is: a ground-up Rust rewrite that is itself a fork of OpenAI's Codex CLI, repositioned as "a coding agent optimized for low-cost models." Headline feature is **harness emulation** — `/harness` lets it impersonate the agent scaffolding of `claude-code`, `kimi-code`, `kimi-cli`, `qwen-code`, `deepseek-tui`, `swe-agent`, etc., on the theory that a cheap/open model performs closer to a flagship model's level when driven by the harness that model's own team tuned against. Speaks the same Codex exec protocol as the Codex CLI already wired into herdr, so wiring should be closer to pointing the existing Codex agent-kind config at the `interpreter` binary than building a new agent-kind from scratch — but that needs an actual test, not just assumed from the README's protocol-compatibility claim.
- Chris's specific interest: the Kimi K3 harness (`kimi-code`) specifically — he wants this evaluated/used as a cheap-model option in the roster, not just installed generically.
- Local Windows-side clone (already cloned + scanned, for reference): `C:\Repos\open-interpreter`, HEAD `5b07159` as of 2026-08-19.
- Security status: `vuln-hunter scan_repo` found 86 leads. Ran a full `fp-check` (not just pattern-matching) on every secret/credential-shaped finding — **13 confirmed FALSE POSITIVES** (test-fixture literals like `"sk-existing"`, a `TEST_`-prefixed synthetic RSA key, an unsigned `alg:none` JWT with fake claims, Cargo.lock dependency checksums, OpenAPI-generated enum tags, a hex-formatted cache-key hash) — **0 TRUE POSITIVES**. Real dependency CVEs exist in `Cargo.lock` (openssl, gix*, quinn-proto, hickory-proto, jsonwebtoken, tar, actix-http, opentelemetry_sdk, cmov) — standard "upgrade the pin" items, only matter once actually built/run.
- **Real vuln-hunter tool bug found during fp-check, worth its own investigation**: 6 of the ~20 distinct secret-finding groups cite file paths that **do not exist anywhere in the repository** (`app-server-protocol/src/protocol/v2.rs` — that's actually a `v2/` directory of ~50 small files, none over 441 lines, nowhere near the cited line 7623; no `core/src/memories/` directory exists at all; `tracing_tests.rs`, `device_key.rs`, and `cloud-requirements/` don't exist anywhere in the tree either). This isn't a security finding — it's a path/line-attribution bug in vuln-hunter itself, on top of the already-known 300s-timeout limitation. Worth a real bug report/fix pass on vuln-hunter separately.
- Install: their documented installer is a curl/PowerShell pipe-to-shell (`irm https://www.openinterpreter.com/install.ps1 | iex` / macOS-Linux `curl -fsSL ... | sh`) — **this got blocked by Claude Code's own auto-mode safety classifier on the Windows side** (piping a remote script into a shell is exactly the pattern it's built to catch, regardless of prior vetting). Hermes should either run the same installer directly inside WSL2 (a different execution context/classifier scope), or build from the already-scanned local source (`cargo build --release` in `C:\Repos\open-interpreter\codex-rs` — port that clone into WSL2's native filesystem first, per the standing `/mnt/c` build-crash rule).

**2. CodeGraph (`github.com/colbymchenry/codegraph`) — install globally across the whole agent roster.**
- What it is: a repo knowledge-graph MCP server (Rust kernel, SQLite storage, single `codegraph_explore` tool) that hands an agent verbatim source + call paths + blast radius in one call instead of a grep/read discovery loop. Benchmarked (2026-08-05 re-measurement, contamination-blocked methodology): 88% fewer tool calls, 53% faster, 62% fewer tokens, 44% cheaper, zero file reads, across 7 real repos.
- **Why this matters for herdr specifically: it natively lists "Hermes Agent" as a supported target** (alongside Claude Code, Cursor, Codex CLI, opencode, Gemini CLI, Antigravity, Kiro, GitHub Copilot) — a real differentiator from jcodemunch-mcp, which is Claude-only. This is the first shared repo-graph tool that could serve the *entire* non-Claude side of the roster the same benefit jcodemunch already gives Claude Code sessions.
- Security status: `vuln-hunter scan_repo` found 31 leads, meaningfully cleaner than open-interpreter's 86 — **no secrets/API-key leaks at all**. 3 XSS-pattern hits are in `__tests__/extraction.test.ts` (test code generating synthetic sample source files, not user-facing input). 3 `strcat` hits are in a vendored/generated `tree-sitter-scala` grammar file, inherited from upstream, not CodeGraph's own code. 4 `child_process` findings (2 in self-upgrade code, 2 in telemetry-dashboard dev scripts) — worth a quick look at the upgrade path specifically, nothing confirmed exploitable. Most dependency CVEs live in `site/package-lock.json` — their separate docs website, not the CLI/MCP server that actually gets installed. Not yet run through fp-check (lower priority than open-interpreter's findings since there are no credential-shaped hits to verify).
- Install: `codegraph install` (interactive, auto-detects installed agents including Hermes; `--target=auto --yes` for non-interactive) wires the MCP server into each agent's config, then `codegraph init` per project builds the actual graph. One global install covers every project. Chris explicitly wants this wired into **all** herdr agents, not just Hermes.
- Deliberately not installed on the Windows Claude Code session this round — jcodemunch-mcp already covers that session's repo-navigation needs, so CodeGraph there would be redundant tooling rather than filling a gap. Its real value is the rest of the roster.

## 2026-08-22 (TIC 2, later still) — Issue #3109 (OpenCode/Kilo Bun SIGILL) closed by maintainer, not planned at herdr's layer

Real outcome, not a false alarm: `ogulcancelik` (herdrdev collaborator) confirmed the exact same Bun 1.3.14 crash and abort address (`0x2607064`) has been independently reported *outside* herdr in two upstream OpenCode issues (`anomalyco/opencode#33890`, `#36280`) -- genuine, reproducible, corroborated bug, not herdr-specific. Also confirmed our corrected diagnosis (real `objdump` disassembly showing `ud2`, a deliberate Bun panic trap, not an AVX-512 illegal instruction) was accurate. What got rejected was the *fix's scope*: applying `BUN_JSC_useJIT=0` globally to every process herdr launches is too broad -- would affect every Bun app in any pane, not just OpenCode/Kilo. Closed "not planned" at herdr's layer, correctly redirected to OpenCode/Kilo (who control their own bundled Bun version) as the actual place to fix it, door left open if herdr-specific evidence ever surfaces. Fork-side fix (`ff7bcc4`) stays as a working local mitigation for our own use; not pursued further upstream.

## 2026-08-22 (TIC 2, later) — herdr Connector built: Claude Code can now call herdr's socket API directly as MCP tools

The "herdr Connector" idea flagged in 3 separate earlier sessions as scoped-but-never-built ("got sidetracked... don't mistake we discussed it for it's in progress") is now real. Evaluated 4 real third-party herdr-socket-to-MCP bridges (eugeneb50/herdr-mcp, 54rt1n/herdr-simple-mcp, islam3zzat/herdr-mcp, bonsai/herdr-mcp) — independently by both Claude Code and hermes-scout, converging on the same pick: **54rt1n/herdr-simple-mcp**. Direct Unix-socket connection (no CLI shell-out), 75/76 tools live against herdr's real manifest, profile-based tool-surface selection (`full`/`coordinator`/`client`/`observer`), single Rust binary, no runtime state of its own.

**Built on WSL2's native filesystem** (`~/build/herdr-simple-mcp`, not `/mnt/c` -- the known crash-risk path), `cargo build --release`, clean build. Scanned clean (`vuln-hunter scan_repo`, 0 findings) -- but the first scan attempt against the `\\wsl$\...` UNC path ran well past vuln-hunter's known 300s timeout and never returned; copying the finished build to a native `C:\Repos\herdr-simple-mcp` path (not a rebuild, just a mirror for shared Windows/WSL2/human visibility) let the same scan finish clean in seconds. Real lesson: the UNC path itself is the slow part for many-small-file operations, not repo size -- prefer a native-path copy for any future scan that needs to run fast.

**Registered in Claude Code's own `~/.claude.json`** as a `wsl.exe`-wrapped stdio MCP server, `coordinator` profile (observe+structure+panes+io+agents+events, 53 tools -- skips `admin`/`authority`, which this use case doesn't need), pointed at the real dev-channel socket (`~/.config/herdr-dev/herdr.sock`, not the stable-channel default the binary assumes). Verified by simulating the exact registered command line directly (not just trusting the config) -- real MCP handshake succeeds. Takes effect on Claude Code's next session restart/reconnect (same hot-reload gap as every other MCP registration this week).

**Net effect:** Claude Code can now call herdr's socket API as direct MCP tool calls instead of shelling out through `wsl.exe bash -lc "herdr ..."` for every single action -- the actual mechanism this whole night's agent orchestration has been working around.

## 2026-08-22 (TIC 2) — Token-savings + Obscura browser stack wired across the full 11-agent roster; aura-code hits a real MCP limitation, worked around with a documented manual step

**Obscura (h4ckf0r0day/obscura) adopted as the shared browser-automation MCP server** for every herdr agent — Rust, headless, own V8 engine, no Chromium dependency. Installed to `~/.local/bin/obscura` + `obscura-worker`, scanned clean, live-verified via a real MCP handshake before wiring anywhere.

**All 11 roster agents now have the full 5-server stack** (jcodemunch/jdatamunch/jdocmunch via `jmunch-mcp`, `fff`, `obscura`) live-verified where a headless test path existed:
- Crush, claw-code, Cline — wired fresh this session, real handshakes confirmed (claw-code's session also found and fixed a real bug: `jmunch-mcp`'s startup chatter breaks its own JSON-RPC handshake unless `UV_QUIET=1`/`UV_NO_PROGRESS=1` are set — ported into Crush's config too).
- Codex, OpenCode, Kilo, Qwen, Hermes, Goose — already had jMunch/fff from earlier sessions, just needed Obscura added; each confirmed live via its own real MCP-list/test command (`codex mcp list`, `opencode mcp list`, `kilo mcp list`, `qwen mcp list`, `hermes mcp test obscura`, `goose info -v`).
- Pi/omp — config matches the same proven schema as the other 10, but omp has no headless MCP-list command (TUI-only), so this one is config-verified only, not live-tested.

**Real gotcha caught mid-edit:** a raw text insertion into Goose's `config.yaml` (adding the obscura block after `fff`) accidentally split `fff`'s own block and misattributed its `timeout: 300` line to `obscura`. Caught via a full YAML parse + per-key structural check before moving on, not just a "does it parse" check. Fixed by restoring the line to `fff`'s block. Lesson: verify structure, not just syntax, after any line-based insertion into an existing YAML/JSON file.

**Real testing-methodology false alarm, also caught and corrected:** an early check of Codex's live MCP list (via `wsl.exe bash -lc`, a non-interactive shell) appeared to show Codex still resolving the broken Windows-mounted `codex` binary instead of the fixed native WSL2 one from an earlier session — this looked like "Codex fully fixed" was never actually true. Re-tested with a real interactive shell (`bash -ic`, matching how herdr actually launches panes) and the native binary resolved correctly. Root cause of the false alarm: `.bashrc`'s own non-interactive early-return (`case $- in *i*) ;; *) return;; esac`), a previously-documented gotcha, striking again in a new context. No fix needed — always verify PATH-dependent findings with an interactive-shell test, not a one-shot non-interactive `bash -lc` call.

**aura-code: a genuine, verified architectural limit, not a config mistake.** Two real fix attempts, both independently verified against aura-code's actual source (`dist/plugins/loader.js`, `dist/plugins/market.js`) rather than trusted from either the tool's own behavior or an agent's self-report:
1. A `.mcp.json` file in a plugin directory (`~/.aura/plugins/<name>/.mcp.json`) — `loader.js` does discover and parse this format, but `market.js` line 269 confirms it's never actually wired to a live connection: `"aura has no MCP client yet, those are ignored."` Real mechanism exists, just not functional yet.
2. A wrapper script piping the 5 `mcp connect` commands via stdin before handing control to the user — fails because aura-code detects non-TTY stdin and falls back to showing help instead of accepting piped commands.

**Real outcome, agreed with Chris as a live experiment:** aura-code stays in the roster. `~/.aura-code/AGENTS.md` documents the 5 manual `mcp connect` commands; the rule "always run them immediately after starting an aura-code pane, before any real task" is now baked into Hermes's own global `coding_instructions` (`~/.hermes/config.yaml`) so every future Hermes-driven aura-code pane gets it automatically, not just this session. If this causes real friction in practice, that's the agreed signal to rotate aura-code out of active use — not desired, but the accepted fallback. Revisit when aura-code (created 2026-07-13, actively releasing — 0.10.4→0.14.0 in ~3 weeks per `npm view aura-code time`) ships native MCP client support.

## 2026-08-24 (TIC 2) — Manager architecture decided after hermes-observer crashed: external ledger, deterministic dispatcher, exception-driven Hermes, scripted observer

Planned in a separate Universal Brain session (not this repo) after the 2026-08-24 agent-roster marathon crashed hermes-observer with a "permanent guardrail halt" and left coordination overhead eating the whole session. Full detail lives in that session's CC memory (`project-herdr-manager-architecture-decision-2026-08-24.md`); this entry is the landing point in herdr's own log.

**Question asked and answered:** is "one Hermes manager supervising N agents" really one task, or N+1 tasks in disguise? Answer, converged three independent ways (ChatGPT, Gemini, and this repo's own docs + a real practitioner's account): it's N+1 *unless state is externalized*. The ceiling isn't agent count — it's how much live state the manager holds and re-reasons over in its own context. herdr's own `/docs/agent-skill/` and `/docs/agent-automation/` pages (initially missed on a first docs pass, found on a second) confirm the herd-skill mechanism itself is real and official; they document zero guidance on how many agents one controller can safely track, which is exactly the gap that bit us.

**Decided architecture:**
- **Ledger:** `nelsonPires5/herdr-board` (Kanban plugin, MIT, trending on herdr's plugin marketplace) — not Hermes's own dashboard-gated board. Confirmed version-compatible: this fork is on herdr 0.8.2, herdr-board requires exactly 0.8.2 (socket protocol 20). Install: `herdr plugin install nelsonPires5/herdr-board --ref v0.16.1`, then `herdr integration install [pi|claude|codex|opencode|antigravity-cli]` per harness. CLI-queryable (`board card list --json`, `board card show <id> --json`), agents call `board card run done --outcome success|failure|cancelled` on completion — a real structured-handoff contract, not prose.
- **Dependency/evidence layer (optional, evaluate alongside):** `aemrebarut/herdr-dagr` — DAG view with evidence tiers (`◆ verified` / `◇ reported` / `≈ heuristic` / `! asserted`), distinguishing an agent's own completion claim from independently verified state. `herdr plugin install aemrebarut/herdr-dagr`, agent-side contract via `npx skills add aemrebarut/herdr-dagr --skill dagr-producer -g`.
- **Dispatcher:** extend the existing `~/.local/herdr-watchdog.py` into this role (claim/heartbeat/stale-worker-reclaim/retry), rather than a second script.
- **Manager (Hermes):** exception-driven — polls a board digest, pulls raw pane output only on blocked/failed/needs-review, never holds all-workers-live-state in its own conversation.
- **Observer:** `tonbistudio/flock-check` — real, open-source Hermes skill, read-only by design (never prompts/restarts/focuses), triggered on-demand or cron, queries `herdr status`/`herdr agent list`/`herdr agent read` mechanically instead of narrating continuously. Install this instead of rebuilding hermes-observer's approach.

**Not yet done — next actual herdr session:** install `herdr-board` + `flock-check` for real, verify herdr-dagr fits alongside the board or is redundant with it, extend `herdr-watchdog.py` toward the dispatcher role, finish the per-agent-kind auto-approve-flag table (partial, several kinds still unconfirmed against real `--help` output).

## 2026-08-24 (TIC 2, same night) — Manager plan actually executed and live-tested; two real gotchas found, fixed permanently

**Status: no longer plan-only.** Same session as the entry above, continued. herdr server started fresh (`setsid nohup herdr server ... & disown`, verified survived via a separate follow-up call, not just the launch itself — the known nohup-without-setsid bug from earlier this week). Confirmed live version matches herdr-board's exact requirement: `herdr status server` → `0.8.2`, protocol 20.

**Installed, all verified working, not just "install command ran":**
- `herdr-board` — `herdr plugin install nelsonPires5/herdr-board --ref v0.16.1 --yes`. `board card list --json` confirmed live and queryable.
- `flock-check` (Hermes skill) — `npx skills add tonbistudio/flock-check -g -a hermes-agent -y` (note: the CLI's agent slug is `hermes-agent`, not `hermes`). **Security check on the install:** the skills installer's own Snyk pass flagged it "Med Risk." Read the actual installed `SKILL.md` directly (single file, 9.6K, zero executable code) and ran `semgrep scan --config auto` on it directly (the MCP `vuln-hunter scan_repo` tool hung on the `\\wsl$\...` path per its documented UNC-path limitation — same fallback as always: copy to a native path, run `semgrep.exe` from vuln-hunter's own venv directly). Result: `"results":[]`, clean in ~7s. The Med Risk flag reads as a heuristic false positive on a markdown file that documents running shell commands, not an actual finding.
- `herdrdev/herdr`'s own official agent skill — `npx skills add herdrdev/herdr --skill herdr -g -a hermes-agent -y`. Snyk: Low Risk.
- `herdr-board`'s own skill contract — not installable via the skills CLI; captured directly via `board skill > ~/.hermes/skills/herdr-board/SKILL.md` (330 lines, the real card/column/run/comment rules).

**Manager-role instructions added to `~/.hermes/config.yaml`'s `agent.coding_instructions`** (edited via Python's `yaml.safe_load`/`safe_dump`, not raw text insertion — this repo already has a real precedent of a raw YAML edit corrupting a neighboring block, see the 2026-08-22 entry above): board-first/exception-driven behavior, plus (added after the smoke test below) the explicit-card-ID and manual-column-closure rules.

**Real gotcha #1: a config/skill edit on disk does not reach an already-running Hermes process.** Same class of bug as jMunch's stale-in-memory-server issue. Verified concretely: asked a live-resumed Hermes instance (session from 2026-08-23) whether it had the new instruction — it correctly said no, and confirmed via its own system-prompt introspection, not a guess. Killed the pane, started a genuinely fresh process, re-asked — "Yes." **Rule going forward, per Chris: after any restart meant to pick up new config/skills, explicitly ask the agent to confirm what it has loaded — don't just trust that "I restarted it" means it worked.**

**Real gotcha #2, found via a live smoke test, not theorized:** created a real card on `herdr-board` (`board card create --title "Manager smoke test" ... --column Todo`), had the fresh `hermes-manager` instance (pane, kind `hermes`) find and complete it. Full loop worked — it used the `herdr-board` skill (confirmed via the tool-call trace, not just "skill is installed"), read `README.md`, correctly extracted the first heading, posted `board comment`. But `board done` failed with exit 2. Root cause, confirmed by reading `board done --help` and reproducing the exact error directly: `$BOARD_CARD_ID` is only preset for herdr-board's own auto-dispatch harnesses (default catalog: pi/claude/codex/opencode/antigravity — Hermes isn't in it), so a manually-polling Hermes must pass the card ID explicitly, always. Separately, `board done` only closes an *active run*; a manual-trigger column has none, so the real closure path is `board move <id> <column>` — and a fresh board may only have one column (`Todo`), so a Done-equivalent column has to actually exist first (`board column create --name Done --trigger manual`). Fixed the specific card by hand, then wrote both rules into Hermes's `coding_instructions` permanently so this doesn't need rediscovering next session.

**Not yet done:** `flock-check` installed but never actually invoked against a live agent (nothing else was running to observe during this test — testing it meaningfully needs at least one more agent live first). `herdr-watchdog.py` still not extended toward the dispatcher role. `herdr-dagr` not evaluated. Per-agent auto-approve-flag table still has gaps.

**Commit/push status:** nothing here touches this repo's own source — this is all runtime config/plugin state (`~/.hermes/`, `~/.config/herdr/plugins/`), nothing to commit in this git repo.

## 2026-08-24 (TIC 2, same night) — herdr-board auto-dispatch: Pi broken, Claude Code too restrictive, Codex needs two real fixes; a genuine OpenCode/Kilo crash breakthrough recovered from an idle Copilot pane

**Auto-dispatch smoke-tested across four harnesses, one at a time.** Uniform (MCP tools, herdr + herdr-board skills, verified auto-approve/permission mode) confirmed for each before attempting dispatch — see [[project-herdr-manager-smoke-test-2026-08-24]] in CC memory for the earlier manual-Hermes-polling test this builds on.

- **Pi — broken, deferred by explicit Chris call, not fixed tonight.** See [[reference-herdr-board-pi-harness-broken-2026-08-24]]. Reproducible spawn failure both reusing an existing workspace and creating a fresh one; likely herdr-board's `pi` harness invokes herdr's own `--kind pi` (a different, unrelated package) instead of the correct `--kind omp`. Chris: "kick it to the curb," revisit at a later date, pick a different agent for now.
- **Claude Code — dispatches correctly (real pane, correct launch args) but hits a hardcoded tool restriction.** herdr-board launches it with `--permission-mode bypassPermissions --allowedTools "Bash(board:*)"` — the allowlist restricts it to *only* `board` commands, so any real task needing Read/Glob/general Bash gets blocked requiring interactive input. Not something a card-level flag fixes; a real limitation of this harness's current dispatch integration. `bypassPermissions` itself is also **not settable as a column default** — herdr-board explicitly requires it be an explicit per-card choice, a real safety guard, not a bug.
- **Codex — three real, different, sequentially-fixed failures, then success:**
  1. First run: Codex auto-updated itself on launch (`npm install -g @openai/codex`) and exited before ever seeing the task — herdr-board has no way to skip/pre-empt this.
  2. Second run (after the update landed): hit `You have no credits remaining` on OpenAI's direct API — herdr-board launches bare `codex` with no provider override, so it fell back to Codex's own configured default (direct OpenAI), not the already-configured CLIProxyAPI route in `~/.codex/config.toml`.
  3. **Fixed for real:** added `model_provider = "cliproxy"` and `model = "nararouter-agnes"` as top-level keys at the very top of `~/.codex/config.toml` (TOML requires bare keys before the first `[table]` header) — makes CLIProxyAPI the persistent default for *any* future bare `codex` launch, not just this card. Retried via `board card run cancel` + `board card run retry` (note: the CLI subcommand is `board card run cancel`, not `board card cancel` — the latter doesn't exist).
- **Antigravity CLI — set up (uniform confirmed, real flag `--dangerously-skip-permissions` verified via `--help`, herdr integration already `current (v2)`), dispatch attempted, outcome pending as of this entry** — check `board card show 7` for the final result.

**Real, substantive finding recovered from an idle pane, not new work tonight — properly credited and preserved.** An old (2026-08-21/22) Copilot instance, sitting idle at `w1:p11` with a stale terminal title ("Fix Opencode-Check Crash"), turned out to have left real investigation artifacts as untracked files in this repo's working tree: `opencode-kilo-crash-investigation.md`, `opencode-kilo-crash-hermes-round.md`, `opencode-kilo-crash-round2.md`. The most complete (`round2`) reaches a materially better-grounded conclusion than this morning's session assumed:

- **Not an AVX-512 illegal instruction** (already suspected wrong this morning) — the actual trap is `ud2` (undefined instruction) at a consistent instruction pointer (`0x2607064`), a deliberate Bun/JavaScriptCore assertion-failure trap, not a CPU incompatibility. Confirmed via real `dmesg` output and disassembly at the crash site.
- **The crash is intermittent** (observed <10% of runs), not deterministic — this alone explains a lot of the confusion in earlier debugging sessions that treated it as reliably reproducible.
- **`BUN_JSC_useJIT=0` (the existing wrapper script's fix) reliably prevents it** in direct, repeated testing — every run with it set exits clean; every run without it is a coin flip. With JIT disabled, JavaScriptCore uses the LLInt interpreter instead, which never reaches the assertion.
- **Conclusion: herdr is not the cause.** This is a genuine upstream Bun/OpenCode runtime bug (a JIT-path assertion failure), independently reproducible outside herdr entirely.
- Added logging to the wrapper (`/home/albat/.opencode/bin/opencode`) to confirm at runtime whether it's actually being invoked and setting the env var correctly when herdr launches it — test results in the file confirm the wrapper does fire correctly.
- Open recommendation from the file itself: verify no other process overrides `BUN_JSC_useJIT` after the wrapper sets it, check for a newer Bun/OpenCode release, and report upstream to the Bun project (with the dmesg output and instruction pointer) if the crash still occurs with the JIT flag explicitly set.

This directly advances the "opencode never got a definitive root cause" open item from this morning's own session log — real progress that had gone unnoticed sitting in an idle pane's working tree until asked about directly. Files are untracked in git as of this entry; worth committing once reviewed, not done yet.

**Next in this session:** stopping the old un-uniformed Copilot instance (w1:p11) once Codex/Antigravity dispatch testing wraps up, then restarting it fresh with the same uniform treatment (MCP, skills, verified auto-approve flag) the rest of tonight's roster got.

## 2026-08-24 (TIC 2, session close) — Copilot uniformed and proven, OpenCode dispatch confirmed still broken, Claw-code hits a new real jMunch handshake failure, session wrapped

**Full harness-catalog result, all 5 herdr-board harnesses now tried:** Codex ✅, Antigravity ✅, Claude Code (dispatches, blocked by hardcoded tool restriction), Pi (broken, deferred), OpenCode (**retried tonight, still fails** — `agent_not_found` spawn error, same symptom as Pi, confirming this is herdr's own OpenCode detection path per this morning's session, not the Bun/JIT crash Copilot's investigation already solved — that fix was never even reached).

**Copilot — uniformed and proven via the manual (non-catalog) pattern**, same as Hermes. Real gotcha found live: `--yolo` did NOT suppress a `board` shell-command confirmation prompt — Copilot sat blocked 2m22s waiting for input despite the flag. Cleared manually (`Down`+`Enter` to select "don't ask again for board in this repo"), which fixed it for the rest of the session. Hit a second, different confirmation gate on the actual `board comment`/`board done` command sequence — approved manually again. Once past both, completed correctly: "Counted 8 .md files" — matches Antigravity's independent count exactly, a real cross-check. Closed via `board move` (no run row, same as every manual-pattern card).

**Claw-code — real, current, unresolved blocker: jMunch MCP handshake fails on all three servers (jcodemunch/jdatamunch/jdocmunch), genuinely hangs, not just slow.** Despite `UV_QUIET=1`/`UV_NO_PROGRESS=1` already being in `~/.claw/settings.json` from the 2026-08-22 fix, every server hit the exact failure that fix was supposed to prevent (`Invalid JSON: Content-Length: ...` — stdout chatter corrupting the JSON-RPC channel). Confirmed genuinely stuck, not just verbose: zero change in pane output across 20+ seconds. Cross-checked against `hermes-manager`'s own clean startup banner the same session (jMunch toolsets active, zero errors) — this looks Claw-code-specific, not an agent-wide regression. Full detail: CC memory `reference-claw-code-jmunch-handshake-broken-2026-08-24.md`. Not fixed tonight — pane closed, revisit next session.

**Session-end cleanup, per Chris's explicit call:** the three old un-uniformed instances running since 2026-08-21 (`hermes-bunfix` w1:p6, `claude-fixer` w1:p5, `hermes-graphql` w2:p4) were stopped — none of them ever got tonight's auto-approve/skill/MCP-verification treatment, so they were left running idle rather than trusted, per Chris's explicit instruction from earlier in the session.

**End-of-session roster state:** `hermes-manager` (w1:p19) and the freshly-uniformed `copilot-worker` (w1:p1D) are the two live agents left running. Everything else from tonight's testing (Claude/Codex/Antigravity/OpenCode/Claw-code smoke-test panes) was cleaned up as each test resolved.

**Full open-items list for next session:** Claw-code's jMunch handshake (new, real, unresolved), OpenCode's herdr-detection path (long-standing, still unresolved — the JIT/Bun crash fix Copilot found is real but moot until detection itself works), Claude Code's `--allowedTools` restriction on herdr-board dispatch, `herdr-dagr` never evaluated, `herdr-watchdog.py` never extended toward the dispatcher role, per-agent auto-approve-flag table still has real gaps (Crush/Cline/Kilo/Aura-code/Gemini-CLI never touched tonight), Pi's `pi`-vs-`omp` harness mismatch still unfixed upstream.

**Commit/push status:** nothing in this session touched this repo's own tracked source — all changes are runtime config/plugin state (`~/.hermes/`, `~/.claw/`, `~/.codex/`, `~/.omp/`, `~/.config/herdr/plugins/`). Nothing to commit here. The recovered OpenCode/Kilo crash investigation files (`opencode-kilo-crash-*.md`, `PLAN.md`, `schema.py`, etc.) remain untracked in this repo's working tree as of session end — worth committing next session after review, not done tonight.

**Role assignment corrected mid-session:** OSS-contribution scouting was first wrongly assigned to a Hermes instance (`hermes-scout`). Corrected: **Pi (omp) is the external OSS scout** — researches GitHub, verifies unclaimed status, reports back with real candidates. **Hermes is the manager and stays inside herdr** — its job is understanding herdr's own capabilities/gaps and overseeing the worker roster, not external legwork. Rationale: information flows through the manager specifically because it has persistent memory, which is the whole reason Hermes was chosen for that role. `pi-scout` (kind `omp`) is now the live scout instance, dispatched through herdr's own agent mechanism (needs a real PTY — connects cleanly to all 5 MCP servers including obscura this way), reading the same persistent log (`/home/albat/.local/oss-scouting-log.md`, inside WSL2, grounded in Chris's real established strategy — substantial ownable chunks, not scattered tiny PRs) the mis-assigned instance had already started populating. That research wasn't discarded, just the ongoing role moved to the correct agent.

**Contrarian discipline established this session** (now a standing Claude Code skill, `~/.claude/skills/contrarian/SKILL.md`) after two real incidents in the same investigation: first, accepting an agent's unverified "no persistent MCP config" claim at face value; second, one layer deeper, accepting my own "I found the fix" conclusion without checking that the discovered mechanism was actually wired to anything. Both caught only by going back to the primary source (the real code) instead of the first plausible explanation.

## 2026-08-21 (TIC 2) — Goose agent support: built, tested, committed, pushed

Added real `Agent::Goose` support (Rust enum variant + `ALL`/`SCREEN_MANIFEST_AGENTS` membership + label/executable/alias lookup + sidebar/sound config + a `goose.toml` detection manifest using patterns captured live from a running Goose session — working state = screen containing `(Ctrl+C to interrupt)`, idle = `Enter to send`). Followed the minimal `Agent::Amp` template (screen-manifest only, no deeper lifecycle integration), since Goose has no session-restore equivalent yet.

**Verified:** `cargo build` clean, `cargo test` 3265/3266 passing (the 1 failure is a pre-existing flaky test — `workspace::tests::generated_workspace_ids_are_short_base32_handles`, random base32 ID length assertion, confirmed unrelated to this change by running it in isolation 3x with different random pass/fail outcomes).

**Committed and pushed, landing independently verified** (not just a clean exit code): commit `e113d03` on branch `feature/goose-agent-support`, pushed to the fork, remote SHA confirmed matching local via `gh api repos/albatrossflyon-coder/herdr/branches/feature/goose-agent-support`.

**Not yet done:** opening the actual PR against upstream `herdrdev/herdr` — held for Chris's explicit go-ahead since it's a public action against someone else's repo, not decided automatically.

**Real build gotchas hit, worth remembering for next time building this from source:**
- `/mnt/c/...` (Windows-mounted path) fails Zig's build cache with `AccessDenied` on rename — build on WSL2's native filesystem instead (`~/src/herdr-build`).
- Copying the repo Windows→WSL2 via `rsync` produces a huge false "everything modified" diff from CRLF/LF line-ending differences alone — check one file's real diff before panicking.
- The vendored `libghostty-vt` dependency needs Zig **≥0.15.2** specifically; Ubuntu's `apt` package (0.14.1) fails with a ZON-import syntax error. Downloaded the official `zig-x86_64-linux-0.15.2` tarball directly (no sudo) and pointed the build at it via the `ZIG` env var.
- `sudo apt-get install zig` hangs invisibly through both the Bash tool and a `!`-prefixed command — neither gives sudo a real controlling terminal. Has to be run in a genuinely separate terminal window by Chris directly.

## 2026-08-21 (TIC 2, later) — OpenCode/Kilo Bun crash: real root cause found, herdr-specific trigger still open

**Confirmed root cause** (via Pi's investigation): both OpenCode and Kilo Code bundle Bun, and Bun's JavaScriptCore JIT emits AVX512 instructions this CPU (Intel Meteor Lake, no AVX512) can't execute, once code tiers up (~2-3s into a session) — real upstream issue `oven-sh/bun#34215`. Crash is `SIGILL`, happens only when the TUI initializes (interactive mode), not in headless/one-shot mode.

**Real fix found and corrected mid-session:** the actual working env var is `BUN_JSC_useJIT=0` (a JavaScriptCore-specific option) — **not** `NODE_OPTIONS=--jitless`, which is a V8/Node.js flag that Pi's first report incorrectly cited as confirmed-working; it does not apply to Bun's JSC engine at all. Confirmed the real fix via the actual `oven-sh/bun#34215` comment thread, not guessed.

**Wrapper scripts installed** (`~/.opencode/bin/opencode` wrapping the renamed `opencode-real` binary, `~/.npm-global/bin/kilo` wrapping the real `@kilocode/cli` bin) that export `BUN_JSC_useJIT=0` before exec-ing the real binary. **Confirmed working standalone** in a plain bash shell — real 8+ second run, exit 0, no crash, well past the crash threshold.

**Root cause of the herdr-specific trigger, found by Pi + Hermes independently:** the wrapper scripts (`~/.opencode/bin/opencode`, `~/.npm-global/bin/kilo`) set `BUN_JSC_useJIT=0` *inside* the wrapper before exec-ing the real binary, so the var never appears in herdr's own pane environment — it's set one process-exec too late to matter for anything herdr itself might read/log, and more importantly this makes herdr's own env the more robust place to guarantee it, independent of which wrapper (if any) PATH resolves to. Both agents converged on the same fix: set `BUN_JSC_useJIT=0` directly in herdr's `apply_pane_launch_env` (`src/pane.rs`), unconditionally for every pane. This is a no-op for non-Bun agents and only affects Bun's own JIT-heavy paths, which barely matters for an interactive CLI.

**Fix applied and fully verified 2026-08-21 (TIC 2):**
- `src/pane.rs` — one line added to `apply_pane_launch_env`: `cmd.env("BUN_JSC_useJIT", "0")`, with a comment citing `oven-sh/bun#34215`.
- `vuln-hunter scan_diff` (deep_review) → **no findings**.
- `cargo test` → **3265/3266 passing** (same pre-existing flaky test as the Goose change above, confirmed unrelated).
- **Live crash-rate verification** (the real test, since the crash was intermittent at <10% before the fix): launched OpenCode through the actual `herdr agent start --kind opencode` path **11 times** and Kilo through `herdr agent start --kind kilo` **9 times**, all against the freshly rebuilt dev binary. **20/20 clean — 0 crashes**, with genuine rendered TUIs confirmed each time (OpenCode 1.18.21, Kilo 7.4.23), not silent failures.
- Chris's explicit precondition ("make sure they're working properly before you commit and push") is satisfied by this result.

**Real correction made mid-session, worth remembering:** started a `claude`-kind pane (`claude-fixer`) to help with this without first flagging that it requires real Anthropic credentials (OAuth or a paid API key) to function at all — unlike every other agent in this roster (Pi/Hermes/Goose all run on free/cheap CLIProxyAPI models). Chris caught this and correctly pointed out the same thing happened in the original 2026-08-19 pilot too (that session's own log shows a `claude-implementer` pane was started already-logged-in, without ever calling out that it consumed real usage). Going forward: always state upfront what account/credentials an agent-start actually runs on, before starting it, not after it hits an auth wall.

## Not yet done
- [ ] Local-only herdr Connector (an MCP server exposing herdr's socket API as tools, registered directly with Claude Desktop, no public exposure) — scoped and agreed (local-first, confirmed by Chris), but not started; got sidetracked into the OpenCode/Kilo crash fix work instead.
- [ ] The PR against upstream `herdrdev/herdr` for the Goose work — commit is ready and pushed to the fork, PR itself not opened.
- [x] OpenCode/Kilo herdr-PTY-specific crash trigger — fixed (`BUN_JSC_useJIT=0` in `apply_pane_launch_env`), scanned clean, tested, 20/20 live launches with no crash. Committed and pushed to `feature/goose-agent-support` on the fork. PR against upstream not yet opened.
- [ ] Qwen Code (`QwenLM/qwen-code`) — real herdr-native agent kind already exists (`Agent::Qwen`, no code change needed unlike Goose), cloned and security-scanned clean (vuln-hunter: no exploitable findings, safe to install). **Not yet installed** — flagged to install next session.

## 2026-08-22 (TIC 1) — claw-code/Crush/Cline agent-kind integration: in progress, real infra gotchas found along the way

**Goal:** add three more native herdr agent kinds (claw-code, charmbracelet/crush, Cline CLI), same pattern as the Goose integration above. Delegated the actual build/wiring work to the herdr roster (Pi=claw-code, Goose=Crush, Hermes=Cline) rather than doing it directly, per Chris's explicit direction to build up the roster's own track record.

**Real infra problems hit and fixed, worth remembering — these cost real time tonight and will recur if not documented:**

1. **`herdr server` started via a one-shot `wsl.exe -e bash -lc "nohup herdr server ... &"` does NOT survive the invocation ending.** `nohup` only blocks the `SIGHUP` signal; it does not detach the process into a new session. When the invoking `bash -lc` script exits, WSL2 can tear down the whole session and everything in its process group receives `Hangup` regardless of `nohup` — confirmed directly in the server log: process started, then `server shutdown initiated` **14 milliseconds later**, all panes exiting with `signal: Some("Hangup")`. This silently killed the server (and all live agents) at least twice tonight, and looked like a WSL2 VM crash/restart at first (misleading — `uptime` showed 0 min once, but the *second* time `uptime` showed a normal 7 min, proving the server death and a VM restart are two separate failure modes that can look identical from the logs alone). **Real fix:** launch with `setsid nohup herdr server > ~/herdr-server.log 2>&1 < /dev/null & disown` — `setsid` creates a genuinely independent session so the server survives the invoking shell exiting. Verified this actually works by checking `herdr status server` from a **separate** subsequent `wsl.exe` invocation after the fix, not just immediately after starting it.

2. **A real WSL2 VM restart (not the above) also happened at least once tonight**, independently, most likely triggered by running 3 concurrent heavy builds simultaneously (`cargo build` x2 + `go build` + `npm install`) against a machine already tight on memory (~2.9GB `vmmemWSL`, ~4.8GB "truly free" system-wide at the time, though Task Manager's "Available" counter — the one that actually matters for real headroom — read closer to 9GB). Real fix going forward: **don't run more than one agent's heavy compile/build phase at a time** — sequence them instead of parallelizing, even though the file-editing/investigation phases are fine to run concurrently.

3. **`core.autocrlf` mismatch between Windows git and WSL2 git on the same NTFS-mounted repo/worktrees produces a false "every file in the repo is modified" `git status` reading**, from *both* environments simultaneously (each sees the other's line-ending convention as a diff). This is NOT real corruption — confirmed twice by checking `git diff --ignore-space-at-eol --stat`, which came back completely empty both times. Don't panic at a mass-modified `git status`; check `--ignore-space-at-eol` before assuming real damage. Setting `core.autocrlf false` in the repo's local config helps for *future* checkouts but does not retroactively fix files already on disk under the old setting — expect the noise to persist on already-checked-out worktrees regardless.

4. **A worktree's `.git` pointer file (`gitdir: <path>`) can only use one path format at a time, and Windows git and WSL2 git disagree on which.** `C:/Repos/herdr/.git/worktrees/<name>` (Windows-style) works from `git.exe` but WSL2's native git treats it as a *relative* path (Linux git has no concept of a `C:` drive letter) and fails with `fatal: not a git repository`. `/mnt/c/Repos/herdr/.git/worktrees/<name>` (WSL2-style) is the reverse — works from WSL2, fails from Windows/PowerShell. Since the actual agent work runs inside WSL2, standardize worktree `.git` files on the **WSL2-style path**, and do diagnostic `git` checks via `wsl.exe -e bash -lc "..."` rather than PowerShell for any worktree under active agent use.

5. **`sudo` cannot be used at all by any herdr-pane agent** — same limitation as the original Goose build gotcha above (`apt-get install zig` hanging), confirmed again tonight trying to `apt install golang-go`: `sudo: interactive authentication is required`, and the pane just hangs/stalls with no way to supply a password. **Always install language toolchains to a user-owned directory** (`~/.local/go`, `~/.local/zig/...`) instead of relying on `apt`/`sudo` — this also has the side benefit of surviving reinstalls more predictably than a system package.

6. **Go and Zig are now permanently installed** at `~/.local/go` and `~/.local/zig/zig-x86_64-linux-0.15.2` respectively, both added to `~/.bashrc` PATH (`# HERDR-INTEGRATION-TOOLS` block) so every future WSL2 shell has them without re-installing. Confirmed both survive a WSL2 VM restart (Zig's install specifically was proven to survive one of tonight's actual restarts).

**Status as of this entry:** claw-code has a real observed-pattern manifest (`src/detect/manifests/claw.toml`) but no Rust wiring yet; Crush has neither a built binary nor wiring (a working cross-compiled Linux binary exists as a fallback at `/mnt/c/Repos/crush/crush-linux`, built from the Windows-side Go toolchain via `GOOS=linux GOARCH=amd64`); Cline needs reinstalling (`npm install -g cline`) since its install didn't survive the restarts, though its manifest may have survived in the `herdr-cline` worktree. Each is being finished in its own isolated worktree (`herdr-claw`, `herdr-crush`, `herdr-cline`, all branched off `feature/goose-agent-support`) to avoid the shared-clone contention that caused problems earlier in the session.

**Note (2026-08-22, TIC 1, later same night):** this "Status as of this entry" note is a stale mid-session snapshot — the actual Crush wiring described as missing above had already been completed in the `herdr-crush` worktree before the WSL2 crash that ended that session; it just never got a follow-up BUILDLOG update. Ground truth for what was actually done always beats a BUILDLOG snapshot that predates a crash — verify the working tree/git diff directly before trusting an in-progress status note as final.

## 2026-08-22 (TIC 1) — Crush agent kind shipped, and the real WSL2 build-crash rule this session nailed down

**Shipped:** `Agent::Crush` merged into `feature/crush-agent-support`, pushed to fork, remote SHA `973bf56` independently verified via `gh api`. Same pattern as Goose: manifest-driven state detection (`src/detect/manifests/crush.toml` — blocked/working/idle rules matched against Crush's real terminal output), plus the mechanical registry additions in `sidebar.rs`, `sound.rs`, `detect/manifest.rs`, `detect/mod.rs`. Scanned clean (semgrep, 51 rules, 0 findings) and tested clean (`detect::` module, 105 passed, 0 failed) before commit. PR against upstream `herdrdev/herdr` **not** opened — same standing rule as the Goose/Bun-fix work, needs a separate explicit go-ahead.

**The real process fix, worth keeping permanently — build any `cargo`/Zig/Go project touching `vendor/libghostty-vt` entirely on WSL2's native filesystem, never on `/mnt/c/...`:**

Building `herdr-crush` directly at its Windows-side worktree path (`/mnt/c/Repos/herdr-crush`) failed twice, for two different real reasons, before this was understood:
1. First failure: a stale system `zig` (0.14.1, from `/usr/bin`, ahead of `~/.local/zig/zig-x86_64-linux-0.15.2` in `PATH`) doesn't satisfy `vendor/libghostty-vt`'s `minimum_zig_version = "0.15.2"` — fixed by pinning `ZIG=~/.local/zig/zig-x86_64-linux-0.15.2/zig` as an env var on the build command (herdr's `build.rs` already supports this override; don't fight `PATH` ordering, just set the var).
2. Second failure, after the Zig fix: `error: failed to rename compilation results ... AccessDenied` from Zig's own build cache — a real Windows-Defender-vs-NTFS-rename race that happens specifically when Zig's `.zig-cache` (or Cargo's `target/`) lives on the `/mnt/c` drvfs mount. `CARGO_TARGET_DIR` alone only redirects cargo's own output, not Zig's separate cache dir nested inside `vendor/libghostty-vt` — so the real fix is copying the **whole worktree** (`rsync -a --exclude target --exclude .zig-cache`) to a native WSL2 path (`~/build/<worktree-name>-src`) and building entirely there. Both cargo's target and Zig's cache end up on ext4, and both failure modes disappear. WSL2 stayed at normal memory usage (~1.3GB) through the full native-fs build — confirming the earlier "3 concurrent builds crash WSL2" gotcha was compounding *this* I/O problem, not a wholly separate one; a single build on `/mnt/c` alone was already fragile before concurrency ever entered into it.

**Sequencing note Chris asked to have documented explicitly:** build **one integration at a time**, not three in parallel worktrees simultaneously — this is what caused the original WSL2 `Wsl/Service/E_UNEXPECTED` crash pattern earlier in the night (see the entry above). Whether 2 *native-filesystem* builds could safely run concurrently now that `/mnt/c` I/O is out of the picture, and now that `.wslconfig` was raised to 11GB memory / 16GB swap, is a genuine open question — not yet tested. Verify with a real two-at-once native-fs build before relying on it, rather than assuming the `/mnt/c` fix alone makes concurrency safe.

**Committing/pushing across the Windows/WSL2 boundary:** committing is cheap (small diff, not the heavy I/O culprit) and was done from the original `/mnt/c/Repos/herdr-crush` tracked worktree, not the scratch native-fs copy — confirmed identical via `git diff -w` on both before committing. Pushing over HTTPS from *inside* WSL2 hung indefinitely (no `gh` CLI installed there, no credential helper configured — a `git push` with no way to authenticate and no TTY to prompt on just hangs, same failure class as the `sudo` gotcha). Fix: push the same branch from the **main Windows-side checkout** (`C:\Repos\herdr`) instead — worktrees share one object database, so the branch and its commits are already there; native `git.exe` already had working GitHub credentials from prior sessions. Landing verified via `gh api repos/albatrossflyon-coder/herdr/branches/feature/crush-agent-support --jq '.commit.sha'`, matched `973bf56` exactly.

**Also fixed as a side effect of the CRLF investigation below:** a stray uncommitted line-ending corruption (mixed CRLF/LF within `src/detect/mod.rs` etc., unrelated to tonight's own edits) was normalized back to pure LF before commit — see the `herdr` section of `LEARNINGS.md` for the full diagnostic trail, since it produced a genuinely confusing false signal (`git diff -w` reading as empty) worth understanding before it recurs.

## 2026-08-22 (TIC 1, same night) - claw-code agent kind shipped

Committed 863d4b4 on feature/claw-agent-support, pushed to fork, remote SHA verified via gh api. Same pattern and same native-filesystem build process as the Crush entry above.

Real bug found and fixed along the way: the bundled claw.toml manifest was written in an old/wrong schema (flat working/idle/blocked arrays) that does not match the current [[rules]] format every other manifest uses - it would have panicked on load. Rewritten to match crush.toml's structure, keeping the same real detection strings (working: the crab-emoji thinking indicator; idle: the input prompt and keybind hints; blocked: the request-failed error text).

Tests: detect:: module, 105 passed, 0 failed, run single-threaded (cargo test defaults to multi-threaded execution across CPU cores, which raced against the shared MANIFEST_CACHE lock and produced misleading cascading failures unrelated to the actual code - not a real bug, a test-runner artifact, see LEARNINGS.md).

Live-verified: real claw v0.1.3 binary built from source via the repo's own install.sh (after fixing a CRLF-corrupted shebang line in that script), launched inside herdr, connected through CLIProxyAPI (openai/groq-oss), herdr correctly detected idle state on a proper agent start. A follow-up manual relaunch (bypassing agent start) dropped out of herdr''s agent tracking entirely - expected behavior, not a bug: manifest-based state detection only applies to panes registered as agents.

Cline remains the last of the three integrations, still pending.

## 2026-08-22 (TIC 1, same night) - Cline agent kind shipped, all three integrations complete

Committed 09673bf on feature/cline-agent-support, pushed to fork, remote SHA verified via gh api.

Real finding: Cline needed almost none of the work assumed at the start of the night. Agent::Cline, the ALL/SCREEN_MANIFEST_AGENTS arrays, sidebar ordering, sound overrides, and the BUNDLED_MANIFESTS registration were all already present in the base branch - Cline has been a fully native herdr agent kind since before tonight. The only real gap was the bundled cline.toml manifest itself: it had a blocked rule and a catch-all working rule, but no idle rule, so Cline could never be detected as idle/ready. Added the missing cline_idle rule matching its actual What can I do for you? ready-state text. Tests: 105 passed, 0 failed.

All three integrations (Crush, claw-code, Cline) now shipped to the fork on their own branches (feature/crush-agent-support, feature/claw-agent-support, feature/cline-agent-support), each off feature/goose-agent-support. None merged into a single branch yet, none opened as PRs against upstream herdrdev/herdr - both still pending a separate explicit go-ahead.

## 2026-08-22 (TIC 1, same night) - Codex CLI fixed and fully wired for herdr use

Real bug found: Codex was only npm-installed on the Windows side (AppData\Roaming\npm) and that install was itself broken for Linux use - missing the @openai/codex-linux-x64 optional dependency, so it threw immediately inside WSL2. herdr already lists codex as a fully native --kind (bundled codex.toml manifest already present, confirmed - no herdr-side wiring needed at all for the agent-kind itself), but the actual binary was never runnable in the WSL2 environment herdr needs it in.

Fixed: npm install -g @openai/codex@latest run natively inside WSL2 (not the Windows side). Real Linux binary now at ~/.npm-global/bin/codex, confirmed via codex --version (0.149.0) and codex doctor (all green: install consistent, git found, sandbox healthy).

Wired the same token-savings stack every other WSL2 agent gets: 4 MCP servers added via codex mcp add (jcodemunch, jdatamunch, jdocmunch, fff, all pointing at the existing WSL2-native binaries already used by Hermes/Pi), plus ~/.codex/AGENTS.md with the same rtk/ponytail instructions OpenCode/Kilo/Qwen already carry. Confirmed via codex doctor: mcp - 4 server (4 stdio) - 0 disabled.

Real finding worth documenting: Codex's custom-provider mechanism (model_providers in config.toml) only supports the OpenAI Responses API wire format - checked the actual WireApi enum in Codex's own source, exactly one variant, Responses, no chat-completions option. Verified live that CLIProxyAPI genuinely speaks this format too (a direct curl to its /v1/responses endpoint returned a correctly-shaped response), so a cliproxy profile was added and confirmed reaching the right endpoint in the right format - groq-oss hit its real 8000 TPM rate limit immediately, expected for anything beyond a trivial prompt. A config quirk also surfaced: profile-specific settings must live in their own $CODEX_HOME/name.config.toml file, not inline profiles.x blocks in the main config.toml - Codex now rejects the legacy inline form outright with a clear error.

Also logged in with a real OpenAI API key via codex login --with-api-key - auth confirmed genuinely reaching api.openai.com (WebSocket handshake HTTP 101, route probe recognizes the key), but that specific account currently has zero API credits, so real completions fail there until Chris adds billing. The cliproxy profile is the working fallback in the meantime, same free-tier-rotation tradeoff every other agent already accepts.

## 2026-08-22 (TIC 1, same night) - aura-code agent kind shipped, first agent-dispatched build of the night

Committed 8c38663 on feature/aura-agent-support, pushed to fork, remote SHA verified via gh api.

First integration tonight actually built by an agent rather than directly - dispatched to Hermes (paired with a Codex verification pane, though the verification pass itself was superseded by the outer session doing its own independent check given time constraints). Hermes installed aura-code for real, ran it, and grep-confirmed its idle (type a task input prompt) and working (thinking spinner) states against real source before writing the manifest - same discipline as the human-built integrations earlier tonight. One real gap caught and fixed: Hermes initially missed src/config/sidebar.rs entirely, correctly diagnosed and fixed once flagged.

Real finding worth logging: checked whether Hermes actually used the token-savings stack (rtk/jMunch/fff) it has wired into its own config.yaml. Partial answer - rtk shows real cumulative usage across tonight but no hook installed (fires only when explicitly typed), and no evidence of jcodemunch/fff MCP tool calls in this specific tasks transcript, just raw grep/sed/cargo commands. Worth fixing rtk init -g before relying on this being automatic for future agent-dispatched builds.

All four integrations (Goose already merged, Crush/claw-code/Cline/aura-code all shipped tonight) now live on the fork as separate branches off feature/goose-agent-support. None merged into one branch, none opened as PRs upstream - still pending a separate explicit go-ahead.
